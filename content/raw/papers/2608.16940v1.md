---
title: "QSimAdv: A Late-Bound, Vendor-Agnostic Architecture for High-Performance Quantum-Circuit Simulation"
source_url: "https://arxiv.org/abs/2608.16940v1"
source_type: "paper"
source_org: "arxiv"
original_lang: "en"
credibility: 4
lifecycle: "latest"
synced_date: 2026-08-22
arxiv_id: "2608.16940v1"
---

## 摘要

Portability in high-performance quantum-circuit simulation need not begin at the kernel. We present QSimAdv, which makes late binding, rather than a common kernel, the basis of vendor independence. Representation, operator lowering, and data placement are bound only when their required inputs become available. Before full-state allocation, circuit, noise, and output inspection can route eligible generic sampled-count requests to a stabiliser tableau; explicitly requested representations remain fixed. For full-state execution, backend constraints shape fusion; an ordered fused operator binds to a native lowering only after its physical targets are known. A first-class logical-to-physical layout map records non-canonical order across local and rank-address bits, so the dispatcher moves nonlocal targets only on demand. GPU, CPU, and Message Passing Interface (MPI) backends share these semantics while retaining native execution paths. We realize this design on NVIDIA GH200 and AMD MI250X/EPYC systems across local and distributed execution. With matched complex 32-bit floating-point state storage, QSimAdv leads both Aer Hopper configurations at $N=32$ and Aer's HIP backend at four shared MI250X sizes from $N=24$ to 30. Strong scaling exposes platform dependence: on setonix, QSimAdv leads both GPU and CPU comparisons at every measured rank, achieving $3.4\times$ and $2.8\times$ speedups, respectively, from one to eight ranks; neither the GH200 path nor the CPU path speeds up at eight ranks. Weak scaling reaches 256 ranks with 2 TiB GPU and 1 TiB CPU states. Together, these results support that portability can reside above the kernel boundary while execution remains native and extends across distributed memory.

## Quantum-Circuit Simulation

Shusen Liu ∗†, Pascal Jahan Elahi ∗†, Wenyun Sun ‡, Shenjin Lv §, Xiaohan Shan ¶, Ugo Varetto ∗† ∗Pawsey Supercomputing Research Centre, Australia †The University of Western Australia, Australia ‡Nanjing University of Information Science & Technology, China §Sina Weibo, China ¶Beijing Zhongke Liangshu Technology Co., Ltd, China Email: pascal.elahi@pawsey.org.au quantum-simulation API on the AMD system evaluated here. Setonix [7] supplies HIP, matrix and tensor libraries, and GPU-aware MPI, yet its ROCm stack does not provide the gate, index-swap, and measurement operations exposed by Abstract—Portability in high-performance quantum-circuit simulation need not begin at the kernel. We present QSIMADV, which makes late binding, rather than a common kernel, the basis of vendor independence. Representation, operator lowering, and data placement are bound only when their required inputs become available. Before full-state allocation, circuit, noise, and output inspection can route eligible generic sampled-count requests to a stabiliser tableau; explicitly requested representations remain fixed. For full-state execution, backend constraints shape fusion; an ordered fused operator binds to a native lowering only after its physical targets are known. A first-class logical-to-physical layout map records non-canonical order across local and rank-address bits, so the dispatcher moves nonlocal targets only on demand. GPU, CPU, and Message Passing Interface (MPI) backends share these semantics while retaining native execution paths. CUSTATEVEC [8]. Translating CUDA syntax to HIP therefore leaves the simulator algorithms themselves to be implemented. QSIMADV treats representation choice and hardware lowering as one late-binding problem. It owns three decisions above the vendor API: semantic routing, fused-operator execution, and the placement of logical qubits in local memory or across MPI ranks. Vendor backends implement four primitives (matrix multiplication, index permutation, sampling, and distributed exchange) without choosing the representation or defining the logical layout. This separation preserves the logical semantics across vendors while leaving each backend free to use native libraries and kernels. We realise this design on NVIDIA GH200 and AMD MI250X/EPYC systems across local and distributed execution. With matched complex 32-bit floating-point state storage, QSI- MADV leads both AER Hopper configurations at N = 32 and AER’s HIP backend at four shared MI250X sizes from N = 24 to 30. Strong scaling exposes platform dependence: on Setonix, QSIMADV leads both GPU and CPU comparisons at every measured rank, achieving 3.4× and 2.8× speedups, respectively, from one to eight; neither GH200 path speeds up at eight ranks. Weak scaling reaches 256 ranks with 2 TiB GPU and 1 TiB CPU states. Together, these results support the central claim: portability can reside above the kernel boundary while execution remains native and extends across distributed memory. The generic full-state lowering has a simple interpretation. View the 2N amplitudes as an N-axis tensor, with one axis per qubit. A fused k-qubit gate acts on k selected axes. Bringing those axes together turns the update into a matrix multiplication; if the axes are not moved back afterwards, a small layout map records their new positions. Specialised kernels preserve the same target and layout semantics without necessarily performing this transform. In a distributed state vector, the high index bits select the MPI rank. Moving a gate target from a rank bit to a local bit is then analogous to bringing a requested item into a cache, which connects the placement problem to offline paging. Index Terms—quantum circuit simulation, vendor-agnostic simulation, stabiliser formalism, state-vector simulation, multi- GPU, MPI, reproducibility I. INTRODUCTION Quantum-circuit simulators provide full-state and intermediate-amplitude references that quantum hardware cannot expose directly. Exact simulation is also a demanding high-performance-computing workload: an N-qubit state vector contains 2N amplitudes, each generic gate touches the full state, and simulations beyond one device must move parts of that state across a communication fabric. We employ the design as late-bound because each architec- tural choice is committed at the last stage with the information needed to preserve the requested semantics. Circuit inspection resolves the representation before a large state is allocated. For a request that remains on the full-state path, the fallback capability selects a backend family and its feasibility limits, including the fusion cap, before fusion. Once a fused operator and its physical targets are known, that backend selects the concrete kernel or transport. Local and distributed layout maps retain non-canonical axis orders until an operation requires materialisation. Several widely used GPU simulators obtain their fastest NVIDIA paths through the closed CUQUANTUM SDK, partic- ularly its CUSTATEVEC state-vector library [1]. These include Qiskit AER’s GPU backend [2], [3], PennyLane LIGHTNING- GPU [4], Google qsim-GPU [5], and CUDA-Q [6]. That route is effective on NVIDIA hardware, but it has no equivalent The source tree has been evaluated on HPC Ella, a Grace Hopper GH200 system with CUDA and HPC-X OpenMPI, and HPC Setonix, an MI250X/EPYC system with ROCm and Cray a) State and layout notation: A circuit C contains gates, measurements, and noise operations on N logical qubits; N denotes its per-operation noise channels, and S is the number of sampled shots. A pure state is a unit vector |ψ⟩∈C2N . The same data can be viewed as a rank-N tensor Ψ, with one binary axis for each qubit. Physical axis positions are numbered by the significance of their flat-index bits, starting with position 0 as the least significant. A layout map L records the physical position of each logical qubit, so a permuted tensor still represents the same logical state. MPICH. The experiments cover semantic routing, single-device CPU and GPU execution, strong scaling, and weak scaling to 256 ranks. A. Contributions This paper makes four contributions. 1) Semantic routing (R1). For generic dense and gpu requests that ask for sampled counts, QSIMADV inspects the circuit before allocating a state vector. Eligible Clifford circuits with Pauli noise use a stabiliser tableau; explicit density-matrix, matrix-product-state (MPS), and tensor- network requests keep their chosen representation. At N = 30, routing the noisy Greenberger–Horne–Zeilinger (GHZ) benchmark is about 78,000× faster than general execution, and the tableau path reaches N = 1000 in under a second. 2) A fused-operator interface (R2). The CPU state-vector path, the CUDA and HIP GPU state-vector paths, rank- local state-vector execution within MPI, and the density- matrix wrapper all accept the same ordered targets, operator, and layout map. The generic implementation combines an axis permutation with general matrix–matrix multiplication (GEMM), while diagonal, controlled, and small-width gates use specialised kernels. On the Hopper random-circuit sweep, QSIMADV overtakes AER without CUSTATEVEC from N = 28, with an AER-to-QSIMADV time ratio of 2.2 at N = 32; on the four MI250X random-brickwork cells shared by all GPU tools, at N ∈{24, 26, 28, 30}, the ratio ranges from 1.15 to 2.16. 3) Distributed layout as explicit state (R3). A logical- to-physical map makes deferred cross-rank placement explicit, and a proved invariant keeps it correct. For a fixed gate stream, the ideal all-frame, full-future demand model with uniform promotion costs reduces to offline paging, where B´el´ady’s rule minimises the number of promotions. The production planner uses bounded lookahead, and the distributed interface reaches 256 ranks and a 2 TiB state. 4) Backend lowering through a shared primitive contract (R4). CUDA, HIP, Arm, x86, and MPI implementations preserve the same representation and layout contracts while choosing native matrix-multiplication, permutation, sampling, and exchange operations. The evaluation char- acterises these lowerings with a kernel decomposition on NVIDIA and a bounding host-region profile on AMD. §II–IV give the background, architecture, and implemen- tation, §V the evaluation, and §VI–VIII the related work, implications, and conclusions; the appendices hold the artefact, measurement protocol, proofs, and complete tables. For distributed execution, let P = 2r be the number of MPI ranks, where 0 ≤r < N. Each rank stores Qloc = N −r local index bits; the remaining r high bits select the rank. The map πt extends L across both local and rank-address bits after execution step t. b) Fused gates and requests: Adjacent operations can be multiplied in circuit order to form a fused k-qubit gate G ∈C2k×2k. Its ordered target tuple is V = (q0, . . . , qk−1), with k = |V |; Kmax denotes the configured fusion cap. The matrix convention is little-endian within this tuple: qj supplies bit j of a row or column index, so q0 is the least- significant target. The generic dense and gpu entry points leave the representation open and name the fallback full- state path. Explicit density-matrix, MPS, and tensor-network requests commit their representation. If semantic routing selects the CPU-resident tableau, that representation-specific implementation takes precedence over the generic full-state fallback. A. Representation classes a) State vectors: A state vector represents arbitrary pure- state circuits exactly up to floating-point error. It requires Θ(2N) memory. Applying a generic dense fused k-qubit operator costs Θ(2N+k) arithmetic on a 2k × 2N−k view of the state, while diagonal and other structured operators can be applied in Θ(2N) work. The exponential state size makes this representation the main target for hardware acceleration and distributed memory. b) Tensor networks and MPS: Tensor-network methods avoid materialising the full state vector, but their cost depends on the sizes of intermediate tensors induced by the contraction order. In an MPS, this dependence appears through the bond dimensions needed to represent entanglement across cuts of the qubit chain [9]. Such methods can be effective when those intermediate dimensions remain small. An MPS result becomes approximate when a bond cap or singular-value cutoff discards weight. QSIMADV includes an MPS engine and an NVIDIA- only CUTENSORNET path; neither is evaluated below. c) Stabiliser tableaux: Clifford gates map Pauli operators to Pauli operators. The Gottesman–Knill theorem therefore permits polynomial-time simulation of Clifford circuits with stabiliser preparations and Pauli measurements [10]; sampled Pauli noise preserves the same structure. QSIMADV uses the Aaronson–Gottesman tableau [11], which represents N sta- biliser and N destabiliser generators by binary coordinates and phase bits. A non-Clifford operation leaves this representation class and requires another engine. Choosing a tableau instead of a state vector changes the asymptotic cost, not merely a backend constant. II. BACKGROUND: REPRESENTATIONS, APIS, AND BACKEND PRIMITIVES Three decisions shape QSIMADV’s execution path: semantic routing selects a representation, the fused-operator interface defines full-state execution, and a layout map records logical- to-physical placement. Vendor backends realise these decisions through four primitives. The underlying tableau and matrix- multiplication algorithms are established methods; the contribu- tion lies in how they are selected and joined across backends. B. How simulators select a representation entry point also identifies its fallback CPU, GPU, or MPI capability, but not a concrete per-operator lowering. R1–R3 define the three above-backend decisions: representation, fused- operator semantics, and qubit layout. R4 defines the primitive contract and feasibility limits through which a selected full-state backend implements them. (R1) Semantic routing. Mature implementations exist for all three classes. State- vector engines include AER [2], LIGHTNING [4], qsim [5], Qulacs [12], QuEST [13], JUQCS [14], NWQ-SIM [15], and Atlas [16]. Stim provides stabiliser sampling [17]; Quimb [18], TensorCircuit [19], and CUTENSORNET-backed engines [1] provide tensor-network paths. Their selection policies differ. A request through the generic gpu or dense entry point asks for sampled counts without pinning the representation. Before allocating a state vector, the router checks whether its circuit, noise, and output semantics support the stabiliser path. An explicit benchmarking switch can disable this route. Density-matrix, MPS, and tensor-network requests retain their requested method and numerical semantics (§II-A). Some tools require the user to choose an engine before the circuit is inspected. Qulacs and QuEST expose state-vector and density-matrix engines, qsim exposes state-vector, hybrid, and MPS engines [20], and Stim is a tableau simulator. The user is responsible for matching the engine to the circuit. Multi-method frameworks may choose automatically. AER’s default method=’automatic’ can select its stabiliser engine for an all-Clifford circuit [3]; an ex- plicit method=’statevector’, device=’GPU’ re- quest keeps the requested method. QSIMADV inspects eligible sampled-count requests made through its generic gpu and dense entry points. These entry points describe a fallback full-state path rather than pinning a representation; explicit density-matrix, MPS, and tensor-network requests retain their requested semantics. Tab. V compares this scope with the other selected systems. (R2) Fused-operator semantics and local layout.

## Full-state engines expose

apply(L, Ψ, G, V ) →(L′, Ψ′), where V is an ordered tuple of logical targets, G is the corresponding fused operator, L bijectively maps logical qubits to physical index-bit positions, and Ψ is the physically stored state. A backend may use a specialised lowering or a generic transpose–transpose–GEMM–transpose (TTGT) path [22]. If a lowering leaves the state in a non-canonical order, L′ records that order. Routing before allocation matters most at the Clifford boundary. The parser can choose the polynomial tableau before creating an exponential state vector, and the selected representation then determines how noise, shots, and output are handled downstream. (R3) Distributed placement invariant. MPI engines extend the layout map across local and rank- address bits, so that after step t the map πt of §II sends each logical qubit to the physical index bit that stores it. The map is a bijection: every qubit occupies one position and every position holds one qubit. If a gate target lies in a rank-address bit, the dispatcher exchanges it with a local bit and records the new placement in πt. Later gates resolve their targets through the map, and output routines either decode the final placement or request canonical order. C. Vendor quantum APIs and toolkit primitives Accelerator platforms expose useful operations at two levels. A quantum-simulation library such as CUSTATEVEC applies gates, swaps index bits, and samples a state vector; CUTEN- SORNET contracts tensor networks [1]. A lower-level toolkit instead provides matrix multiplication, tensor permutation, random numbers, reductions, and communication. NVIDIA supplies both levels. CUDA offers cuBLAS and cuTensor, while CUQUANTUM adds the quantum operations used by several simulators. AMD supplies the toolkit level through HIP, hipBLAS/hipBLASLt, hipTensor, rocRAND, and GPU- aware MPI, but the evaluated stack has no direct analogue of CUSTATEVEC or CUTENSORNET. (R4) Backend lowering through a shared primitive contract. CUDA, HIP, CPU, and MPI implementations provide four primitives (matrix multiplication, index permutation, sampling, and distributed exchange) plus specialised gate kernels. If routing retains a full-state path, the fallback capability selects the backend family before fusion. That family supplies feasibility limits, including the fusion cap and distributed chunk width. Once a fused operator and its physical targets are known, the backend selects the concrete kernel or transport. Fig. 1 traces the pipeline from an OpenQASM 3.0 circuit [21] to sampled counts. Parsing first removes operations that cannot influence any measured qubit, that is, operations outside the measured outputs’ causal cone. Eligible Clifford circuits with Pauli noise then take the tableau path without allocating a 2N state vector. For a circuit that remains on the full- state path, the engine factory selects a compatible backend family and its limits before commute-aware reordering and fusion. The resulting blocks execute through the R2 fused- operator interface; MPS and tensor-network engines keep their representation-specific semantics. QSIMADV targets the toolkit level. Its common interface describes gates, tensor axes, and layouts; CUDA, HIP, CPU, and MPI backends implement the required operations with their native libraries or kernels. Throughout, vendor-agnostic means that the layers above the backend name logical qubits, tensor axes, matrices, and layout maps rather than vendor handles or operation descriptors; a backend still chooses its own libraries, arithmetic modes, kernels, and feasibility limits, provided that it preserves the selected representation and layout semantics. §III defines this interface, and §V measures its behaviour on both vendor stacks. III. ARCHITECTURE A run begins with a parsed circuit C, noise model N, requested entry point, and shot count S. A generic full-state representation is selected before backend capability Clifford stabiliser representation tableau eligible semantic classifier (R1) tableau engine OpenQASM 3.0 Parse + lightcone circuit reduction CPU bitwise / SIMD updates otherwise Commute-aware reorder + DP fusion

## Engine factory

Full-state engines sharing the fused-operator contract

## GPU
state vector

density matrix via a 2N-axis SV core distributed layout (R3) Vendor-agnostic state-vector contract (R2) Backend and transport lowerings (R4) CPU: permute + BLAS

## 2. Fused update
matrix multiplication

CUDA: cuTensor axis order and defer restoration + cuBLAS HIP: bit permute or structured kernel + hipBLASLt MPI: rank-bit exchange Fig. 1. System pipeline. Routing selects the representation before large state allocation. MPS and tensor-network engines retain representation-specific paths. Here SV denotes state vector and DP denotes dynamic programming. one-qubit Pauli form permits unequal X, Y , and Z probabilities. The implemented checker recognises bit flip, phase flip, bit- phase flip, depolarising noise, phase damping in its I/Z- mixture form, and biased one-qubit Pauli noise; multi-qubit depolarising noise is uniform over its non-identity Pauli strings. Amplitude damping and generic custom Kraus channels are not eligible, and the router does not inspect a custom Kraus list to infer that it happens to define a Pauli mixture. A single pass over the parsed circuit performs the eligibility check; any unsupported operation keeps the request on the full-state path. The tableau is CPU-resident, and its arrays of 64-bit integers are updated through AVX-512, AVX2, SVE/SVE2, NEON, or scalar kernels. A CUDA or HIP request that takes this route therefore allocates no GPU state vector.

## IV. IMPLEMENTATION

The implementation follows the architecture’s late-binding order: it resolves the representation before allocation, selects the full-state backend family and its limits before fusion, and chooses each concrete per-operator lowering after the fused block and its physical targets are known. A. Semantic routing and engine selection (R1) The generic gpu and dense entry points specify a fallback full-state path but leave the representation open. For sampled- count requests, R1 may instead select a tableau when the circuit is eligible. Explicit representation requests keep their selected method. If routing retains full-state execution, the fallback capability selects a compatible CPU, GPU, or MPI backend family and the router checks its size limits. For an eligible circuit, QSIMADV uses an Aaronson– Gottesman tableau [11]. The tableau has N destabiliser rows and N stabiliser rows, each containing 2N binary symplectic coordinates and one phase bit. The implementation stores each symplectic column and the phase column in its own array of 64-bit unsigned integers. Within any such array, row s, for 0 ≤s < 2N, occupies bit s mod 64 of element ⌊s/64⌋; each column therefore uses ⌈2N/64⌉array elements. A one- or two-qubit Clifford gate applies a fixed sequence of bitwise operations to a constant number of columns. A circuit is tableau-eligible when every operation remains within Clifford dynamics: Clifford gates, rotations equivalent to Clifford gates, Pauli measurements, and supported Pauli noise channels. A Pauli channel is a classical mixture of tensor- product Pauli errors. For each shot, QSIMADV samples one error from every such mixture. Pauli operators are themselves Clifford operators, so inserting the sampled errors into a Clifford circuit leaves the entire trajectory Clifford and exactly representable by a tableau [10]. The channel output is generally a mixture rather than one stabiliser state, but the ensemble of tableau trajectories is exact: averaging their conditional measurement distributions gives the noisy output distribution. Pauli noise therefore retains the polynomial stabiliser path rather than forcing a full-state representation. Let WClifford(N) count the 64-bit integer updates needed for one such gate. Then WClifford(N) = Θ = Θ(N). (1) For measurement, the implementation forms a row-oriented copy in which a generator’s N X-coordinates and N Z- coordinates occupy 2⌈N/64⌉64-bit integers. A measurement The noise need not have one global error rate. Channel parameters are attached to individual circuit operations, and the can combine O(N) such generator rows. Charging the |C| cir- cuit operations at the gate-update cost and adding measurement and initialisation work gives the conservative per-shot work, measured in the same unit: where ⊕is bitwise XOR. The Z component changes only phases and therefore leaves computational-basis probabilities unchanged. Each X component on a measured qubit flips the corresponding output bit; on unmeasured qubits it only permutes traced-out outcomes. |C| · N + (nmeas + 1) · N 2 Wshot(C, N, nmeas) = O , (2) Accordingly, XORing a noiseless output sample with fX,M yields a sample from the trajectory conditioned on F. Averaging Eq. (3) over frames drawn from the Pauli-channel mixture recovers the noisy distribution Prnoisy(z) = EF [Pr(z | F)]. where nmeas counts measurement operations; for the terminal- readout circuits evaluated it equals the number of measured qubits. The added unit accounts for constructing the tableau. The fast path below constructs the noiseless tableau once and reuses it across shots, amortising this initialisation cost. QSIMADV constructs the noiseless tableau once and propa- gates up to 64 independent Pauli frames together. Bit b of each 64-bit integer records the X or Z component for shot b, so one Clifford update advances the whole batch. The implementation then samples one ideal terminal record and applies the XOR correction for each active frame. Thus S shots require ⌈S/64⌉ batched frame walks and S noiseless samples, with no nested trajectory loop. Tableau-eligible circuits outside this terminal- readout regime use Algorithm 1. Appendix C-H describes the synthetic benchmark, which uses the fast path. Algorithm 1: One stabiliser shot under semantic routing. T denotes the Aaronson–Gottesman tableau; E denotes a per-operation Pauli channel. Input: Circuit C, classified as tableau-eligible; Clifford gates g and per-operation Pauli channels E. Each sample σ ∼E is a Pauli operator drawn from the corresponding channel mixture. Output: Per-shot measurement record {bq} for B. Fused-operator interface and lazy TTGT (R2) measured qubits q. For full-state execution, R2 fixes the semantics of a fused operator while allowing several physical kernels. Given (L, Ψ, G, V ), a backend resolves the ordered physical target 1 T ←initialise the tableau for 0N 2 for op in C do 3 if op is a Clifford gate g then positions A = (L(q0), . . . , L(qk−1)), applies G in the little- endian order fixed by V , and returns a state-layout pair that preserves the logical state. Diagonal, controlled, small-width in-place, and custom dense kernels handle common cases. The generic TTGT-style fallback moves the target bits into the matrix-row block, reshapes the state as M ∈C2k×2N−k, and computes M ′ = GM. The updated layout map records the retained forward permutation, allowing the inverse data permutation to remain deferred (Fig. 2). 4 apply g(T ); 5 if g carries a Pauli channel E then 6 sample σ ∼E; apply σ(T ); 7 end 8 else if op is a standalone Pauli channel E then 9 sample σ ∼E; apply σ(T ); 10 else if op is a measurement on qubit q then 11 bq ←measure(T , q); 12 end Fusion and placement operate on fused blocks and layout metadata; they do not assume that every block executes through GEMM. 13 return {bq}; For terminal computational-basis measurements, sampled Pauli errors need not trigger a full tableau replay. Clifford gates map Pauli operators to Pauli operators, so the errors can be propagated to the end of the circuit and combined into one Pauli frame, A tensor in permuted physical order remains a valid state representation as long as L records the physical index-bit position of each logical qubit. For the generic permutation– GEMM lowering, applying G can be written as ΠA 7−−−→eΨ G⊗I2N−k 7−−−−−−−→eΨ′ Π−1 A 7−−−→Ψ′ rest, N−1 Y XfX,j j ZfZ,j j , fX, fZ ∈{0, 1}N, F = ω where ΠA sends the target at L(qj) to physical position N −k + j. Thus qj becomes bit j of the matrix-row index; in a row-major tensor display the leading target axes appear as qk−1, . . . , q0. The state Ψ′ where fX,j and fZ,j record the X and Z components on qubit j and ω is an irrelevant global phase. (The XjZj ordering fixes the sign of a Y component.) rest restores the input order. The restored branch returns (L, Ψ′ rest). The lazy branch stops at eΨ′ and returns (L′, eΨ′), with L′ recording the retained permutation. This reduction assumes one terminal joint computational- basis readout. All sampled noise must precede the first recorded measurement, with no reset, adaptive classical control, or mid- circuit dependence on a measurement outcome. Under this reshape, if bj is the value of target qj, its row number is Pk−1 j=0 bj2j. The columns enumerate assignments to the remaining bits. This is the basis order used by G, so left multiplication by G applies the fused gate independently to every column. Let M = (q0, . . . , qm−1) be the ordered list of terminally measured qubits, and let fX,M = (fX,q0, . . . , fX,qm−1) be the restriction of the frame’s X component to those qubits. For every measured output string z ∈{0, 1}m, The data permutation ΠA rearranges amplitudes; let αA denote the same rearrangement at the level of physical bit positions. Thus, a logical qubit stored at position b before the z | F z ⊕fX,M = Pr noiseless , (3) 1. Stored rank-N tensor identity layout L(q) = q; targets V = (2, 4) Algorithm 2: Generic axis-shifted (lazy-TTGT) lower- ing of a fused k-qubit operator. L maps logical qubits to current physical index-bit positions. The dense branch materialises the forward permutation ΠA and records its resulting layout as L′ = αA ◦L; the omitted data permutation Π−1 state tensor Ψ N −1, . . . , 5 4 3 2 1 0 ΠA: form the target row block A and corresponding layout repair α−1

## 2. Permute and flatten

A are deferred. The diagonal branch leaves the layout unchanged. M 2k × 2N−k 4 2 N −1, . . . , 5 3 1 0 2k rows 2N−k columns Input: State tensor view Ψ with layout map L; gate matrix G on ordered logical target list V = (q0, . . . , qk−1), with q0 least significant; mode ∈{dense, diag} Output: Updated layout map L′ and stored state view apply M ′ = GM

## 3. Apply G to the row index

G 2k × 2k × M 2k × 2N−k = M ′ 2k × 2N−k 1 A ←(L(q0), . . . , L(qk−1)); reinterpret the same buffer 2 if mode = diag then

## 4. Updated state and layout

3 eΨ′ ←multiply each amplitude by the diagonal stored tensor eΨ′ layout metadata L′ = αA ◦L and row-major axis order (4, 2, N −1, . . . , 5, 3, 1, 0) entry indexed by the target bits at positions A; 4 L′ ←L; A is deferred; no im- mediate data movement 5 else 6 choose αA and ΠA that place target qj at row-index bit j; Fig. 2. Lazy TTGT for the identity layout L(q) = q and targets V = (2, 4). Axes are shown from most to least significant, so the target row block is (4, 2) although q0 = 2 is its least-significant bit; L′ records the retained 7 eΨ ←ΠA(Ψ); 8 view eΨ as M ∈C2k×2N−k; permutation. 9 M ′ ←GM; 10 eΨ′ ←tensor view of M ′; 11 L′ ←αA ◦L; permutation is stored at position αA(b) afterwards. Algorithm 2 records this change as L′ = αA◦L. This is the gate-application form of TTGT [22], with the trailing transpose represented in L′ instead of executed. We call the generic fallback lazy TTGT; specialised R2 kernels need not use GEMM. 12 end 13 return (L′, eΨ′); For the generic lowering, the cost model separates explicit permutation traffic from arithmetic. We write V for a data- movement volume measured by the number of complex amplitudes transferred. When materialised, the permutation moves The density-matrix engine reuses this contract by repre- senting an N-qubit density matrix ρ ∈C2N×2N as a vector with 2N binary axes. Column-major vectorisation stacks the columns of ρ into vec(ρ) ∈C22N : the lower N bits of the vector index select a matrix row, and the upper N bits select a matrix column. For a unitary U ∈C2N×2N , this convention gives Vperm(N) = Θ(2N) complex amplitudes (4) through memory (the whole state, once); the copy is elided when the targets already occupy the matrix-row positions in the required order. The GEMM performs vec(UρU †) = (U ⊗U) vec(ρ), Wdense(N, k) = Θ(2N+k) complex multiply-adds. (5) where U denotes entrywise conjugation. A channel with R Kraus operators Ei ∈C2k×2k is represented after vectorisation by PR For a diagonal fused operator, the permutation and GEMM collapse to a streaming elementwise multiply: i=1 Ei ⊗Ei. Each Kronecker term is a 4k × 4k matrix containing 16k entries, so materialising and summing all R terms costs Θ(R · 16k). A logical k-qubit channel therefore acts on 2k axes in the doubled representation. For each ordered target tuple, the wrapper passes the row-copy positions first and the column-copy positions second, preserving target order within each group. The original factor acts on the lower row bits, and the conjugate factor acts on the more-significant column bits. The implementation constructs the dense superoperator once. Replacing N by 2N and k by 2k in Eq. (5) gives Θ(22N+2k) application work; the vector and superoperator contain 4N and 16k entries, respectively. Wdiag(N) = Θ(2N). (6) QSIMADV exposes these two scheduling terms above the backend API; CUDA, HIP, and CPU backends implement them with different libraries and constants. The two terms are profiled separately on GH200 (Appendix C-F); Appendix C-B lists the backend-specific fusion caps, including the Hopper setting evaluated in §V-C. The fuser, density-matrix wrapper, and rank-local dispatcher used during MPI execution submit fused operators with explicit target order; each backend then selects a lowering. C. Fusion on the state-vector interface the windows are partitioned, a greedy pass attempts to merge blocks across their boundaries. The complete reorder-and-fuse pipeline is repeated at most four times and stops earlier when the gate count no longer decreases. §V-C measures the choice of Kmax. A diagonal-aware DP score changes the measured Hopper intervals by less than 2% (Appendix C-B); the diagonal gain comes from the streaming kernel rather than the partition score. Fusion replaces consecutive eligible gates by their matrix product. A wider fused block reduces the number of gate applications and layout changes, but its matrix grows with the number of distinct target qubits. QSIMADV first performs a semantics-preserving reorder, moving a gate only across disjoint or commuting neighbours, and then chooses the block boundaries with a deterministic dynamic program. Within a block, matrices remain in circuit order, with later gates multiplied on the left. Each operation carries a conservative marker for eligibility for the streaming diagonal lowering. A block has mode diag when every constituent operation carries that marker and mode dense otherwise. D. Distributed logical-to-physical placement (R3) A distributed layout can be viewed as a cache. Local index- bit positions are the cache slots, logical qubits are the items, and each fused gate requests its target qubits. A target currently stored in a rank-address bit causes a promotion: the dispatcher exchanges that bit with an available local position. Retaining the new placement avoids an immediate reverse exchange and makes the future gate stream the request sequence for the paging model below. For a block of width k ≥1, the scheduling score is γ(k, dense) = 4k γ(k, diag) = 1, where k is the number of distinct target qubits in the block. The dense score is the number of entries in its 2k × 2k matrix. A diagonal block receives unit score because it can use a streaming elementwise kernel. These scores guide the partition rather than predict its runtime. A state vector distributed across P = 2r ranks (0 ≤r < N) keeps Qloc = N −r of its N index bits local to each rank; the remaining r bits are rank-address bits. Let |ψ⟩denote the global state and ψx = ⟨x|ψ⟩its amplitude at flat basis index 0 ≤x < 2N. For rank p ∈{0, . . . , P −1}, let ψ(p) ∈C2Qloc Only unitary gates without attached noise enter the dynamic program. A barrier, measurement, reset, control-flow marker, or noise-bearing gate ends the current fusible run and passes through unchanged. A run longer than W = 64 gates is divided into consecutive optimisation windows of at most W gates, bounding the quadratic search. The evaluated configurations cap dense blocks at Kmax and marked diagonal blocks at a backend-specific value no greater than Kmax. An input gate wider than its applicable cap is retained as a singleton rather than rejected. We therefore use Kblk for the larger of Kmax and the width of the widest eligible input gate; it bounds the width of any emitted block in these configurations. be its local amplitude vector and ψ(p) j its entry at local offset j. A canonical block distribution assigns j = ψp·2Qloc+j, 0 ≤j < 2Qloc. (8) Thus rank p owns a contiguous range of global amplitudes. The r high physical index bits hold the rank address p, and the Qloc low bits hold the local offset j. A gate whose target maps to a rank-address bit requires data movement before a local kernel can apply it. Subsequent gates can retain that placement as long as dispatch and output use the updated map. The dispatcher stores a bijection from logical qubits to physical index bits, Consider one window containing h ≤W reordered gates g0, . . . , gh−1. Let B(j, i) be the candidate block formed by the consecutive gates gj, . . . , gi−1. Its width k(B) is the number of distinct target qubits, and its block score is Γ(B) = γ(k(B), mode(B)). Let Di be the minimum score for partitioning the first i gates in the window, with D0 = 0. If the final block begins at j, the preceding blocks contribute Dj and the final block contributes Γ(B(j, i)). Let Ji contain every j ∈{0, . . . , i −1} for which the width of B(j, i) does not exceed the cap for its mode, and always include i −1 so that an input gate wider than its fusion cap remains an unfused singleton. Therefore πt : {0, . . . , N −1} →{0, . . . , N −1}, together with its inverse. The step index t advances on every promotion and fused-operator application. To specify the direction of the map, write a basis index x in bits x0, . . . , xN−1, with x0 least significant. Let y be the bit string obtained by placing logical bit xq at physical position πt(q), so that yπt(q) = xq for every logical qubit q. The corresponding permutation operator is defined on computational-basis states by , 1 ≤i ≤h. (7) Di = min Dj + Γ(B(j, i)) Ππt |x⟩= |y⟩. Thus Ππt carries logical bit q to physical position πt(q). For a fused operator on V = (q0, . . . , qk−1), the ordered physical targets are At = πt(V ) = (πt(q0), . . . , πt(qk−1)). For each endpoint i, the recurrence tests every feasible start j of the final block; backtracking from Dh recovers the chosen boundaries. The search costs O W2 · Kblk per window and O(|C| · W · Kblk) over a circuit with |C| parsed instructions. Constructing a dense product for a multi-operation block of width k ≤Kmax by conventional matrix multiplication costs O If a target b ∈At is a rank-address bit, the dispatcher exchanges it with an unpinned local index bit ℓ. If s of the k targets are rank-address bits, then k−s local targets are pinned. Whenever k ≤Qloc (the hypothesis of Theorem 1 below), the number of available local bits is Qloc −(k −s) ≥s, so all promotions are feasible. 8k , giving the conservative circuit-wide bound O |C| · 8Kmax in Tab. VI. An over-cap singleton reuses its input matrix and is not included in this construction term. After Write |ψt⟩for the canonical logical state after step t, the state a canonically ordered execution would hold, and Algorithm 3: Logical-to-physical MPI dispatch. Place- ment metadata retains the resulting non-canonical order. A later consumer may decode the map or request materialisation. for the state as physically stored. The tilde marks permuted storage order, exactly as for the axis-permuted tensor eΨ of §IV-B. Equation (8) applies to the amplitude vector of Input: Fused operator G on V = (q0, . . . , qk−1); the current map π = πt and its inverse; local count Qloc. Output: G applied to the distributed state; maps Theorem 1 (Distributed placement invariant). Assume an initial bijection π0 and stored state satisfying = Ππ0 |ψ0⟩, and assume every fused operator acts on at most Qloc qubits. After each step t, the stored and canonical states satisfy updated. // Precondition: k ≤Qloc. 1 A ←(π(q0), . . . , π(qk−1)); 2 Pin ←{b ∈A : b < Qloc} ; // already-local target positions, pinned = Ππt |ψt⟩. (9) 3 foreach b ∈A with b ≥Qloc do 4 choose ℓ∈{0, . . . , Qloc −1} \ Pin by the Proof sketch. The base case is the assumed initial invariant. A distributed bit transpose swaps two physical index bits; composing πt with the same transposition preserves Eq. (9). Once all targets are local, write their ordered physical positions as A = πt(V ). Applying the physical fused operator to A is equivalent to applying the logical operator to V ; a lowering that retains the local axis relabelling αA (recording L′ = αA ◦L, §IV-B) is absorbed the same way a promotion is. Let ˆαA denote the extension of αA by the identity on rank-address bits; updating πt+1 = ˆαA ◦πt then records the retained relabelling. Induction over promotions and fused operators proves the invariant. Appendix C-D gives the component-wise form. placement policy; 5 perform the selected distributed lowering for (b ↔ℓ); 6 update π and π−1 by exchanging physical positions b and ℓ; 7 replace b by ℓin A and insert ℓinto Pin; 8 end 9 apply G to ordered local positions A through the R2 interface; 10 if the selected R2 lowering changes the order of local bit positions then 11 compose π with that returned position permutation, leaving rank-address positions fixed; rebuild π−1; 12 end The production planner may choose a nonidentity π0 before execution. A metadata-only relabel is valid for the permutation- invariant initial state used here; an arbitrary supplied state must first be physically reordered or otherwise made to satisfy the theorem’s initial invariant. corresponding per-rank communication volumes, measured in complex amplitudes, are A consumer may either decode its result through πt or request a physical layout compatible with its operation. In particular, terminal computational-basis count sampling decodes logical output bits through the final map and does not require canonical amplitude order. Veager = 2ν Vlazy = ν i + pout Algorithm 3 lists this dispatch. Once its targets are local, a dense k-qubit block costs Θ(2Qloc+k) arithmetic per rank, obtained by replacing N with Qloc in Eq. (5); structured blocks may use cheaper lowerings. The evaluated configurations use the backend-specific fusion caps of Appendix C-B and satisfy the distributed chunk-width constraint of Appendix C-C. The factor two in Veager is the forward-and-reverse round trip. Lazy execution does not pay that return immediately: the promoted target remains local and may make a later pL i zero. The two sequences of counts need not be equal because they are evaluated under different evolving layouts. For permutation- aware terminal count sampling, the output is decoded through the retained map and pout = 0. The following model counts promotions one target position at a time. One promotion exchanges a rank-address target with a local position and sends ν = 2Qloc−1 amplitudes per rank. Across all ranks this is Θ(2N) amplitudes, up to transport- dependent constants. Corollary 1 (Ideal minimal demand-promotion count). Treat the Qloc local positions as page frames and the logical qubits as pages. Fix the fused stream and initial frame contents, with distinct targets in every tuple and at most Qloc targets per tuple. A missing target is promoted only on demand, at uniform cost, Suppose the backend receives fused blocks G1, . . . , GJ. In the eager baseline, let pE i be the number of forward promotions used to make the targets of Gi local. Immediately after applying Gi, the baseline reverses those exchanges, so the block ends in the layout in which it began. In the lazy execution, let pL by evicting one local bit; any bit not targeted by the current tuple is eligible. Let pi be the number of promotions made for target tuple i. The policy knows the full future and does not prefetch. Call this the all-frame demand model. Within this model, evicting the eligible bit whose next use is farthest in the future minimises PJ i be the number of promotions required by Gi from the layout retained after Gi−1, and let pout count any additional cross- boundary exchanges demanded by the final consumer. The i=1 pi. b) CPU lowerings: The CPU state-vector engine can apply a narrow fused block directly to amplitude groups instead of first permuting its target axes. Fixing the N −k non-target bits identifies one independent group of 2k amplitudes. A direct kernel gathers this group in the ordered-target convention of R2, computes the matrix–vector product with G, and writes the result back. Independence between groups permits OpenMP parallelism. Within a group or across adjacent groups, the same short sequence of complex multiply-adds recurs, making it suitable for single-instruction, multiple-data (SIMD) execution. Explicit NEON kernels cover one- and two-qubit updates, AVX2 covers one-qubit updates, and the compile-time matrix dimensions for k = 3, 4, 5 allow the compiler to unroll and vectorise the loops; scalar implementations remain available. Justification. Let Vi denote the ordered target tuple of the i-th fused operator. Expand each tuple into its constituent qubit references, write that list twice, and concatenate the resulting lists. The block structure of this ordinary scalar reference string is V1, V1, V2, V2, . . .. Every reference in the first copy of a block has its next use inside that block, whereas a frame not targeted by the block is next used no earlier than the following block; farthest-next-use therefore never evicts a current target while the first copy is being served, and the second copy is all hits. Doubling also preserves the next-use order of all unpinned frames. Conversely, a pin-respecting batched policy can pay its batch misses while serving the first copy and then serve the second copy entirely from the resulting cache, so its batched promotion count equals its fault count on the doubled string. The batched pinned problem therefore has the same minimum as ordinary offline paging on the doubled string. Farthest-next- use eviction, introduced by B´el´ady [23] and proved optimal in Mattson et al.’s framework [24], minimises the fault count on that string. For a narrow block, a target-axis permutation, an auxiliary state buffer, and a general matrix multiplication call can cost more than the local arithmetic. The dispatcher therefore streams diagonal blocks in place, uses direct dense kernels for k ≤3 and for four- and five-qubit blocks beyond a state-size crossover, and otherwise uses host permutation followed by complex GEMM. Direct kernels assume the canonical flat- index layout, where qubit q occupies index bit q, so the engine first materialises any pending lazy permutation. The generic branch may instead leave its new order represented in L. Both branches implement the same R2 semantics. Appendix C-A gives the concrete crossover used in the evaluated builds. The model minimises promotion count only: it prices neither message shape, topology, protocol overhead, nor overlap. The production GPU-MPI planner approximates farthest-next-use over a 1024-entry schedule window and restricts victims to the first Qloc−1 local positions. CPU-MPI uses first-free placement, the lowest-indexed unpinned local position. On the strong- scaling fused stream, the GPU window sees all remaining references, while the restricted victim set still separates the implementation from the corollary; §V-G quantifies the policies. The tableau’s bitwise operations on 64-bit integer arrays have a separate instruction-set dispatch across AVX-512, AVX2, SVE/SVE2, NEON, and scalar implementations. CPU-MPI applies the same local state-vector dispatch after R3 brings the targets into local index bits; its first-free victim policy remains the one defined in §IV-D. Placement selects which axes to promote; chunked dispatch moves them through intra-rank index swaps and XOR-partner or coalesced cross-rank transfers. Diagonal and controlled operations retain their fast paths, and a full-permutation fallback may use MPI_Alltoallv. F. Complexity summary Representation determines the exponential state size, while fusion and placement determine arithmetic intensity and data movement. Tab. VI collects the bounds derived above. E. Backend lowering through the shared primitive contract (R4) After the router selects a method and the layout layer resolves its targets, the backend executes the four backend primitives. Tab. I lists the implementations used by the common state- vector path. Bounded-support fusion, GEMM-based gate application, tableau updates, and high-bit/low-bit state partitioning are established techniques [25], [26], [27], [11], [14]. QSIMADV combines them behind representation and layout contracts. The distinct distributed contribution is the explicit placement invariant and the offline-paging reduction under the model of Corollary 1. §VI compares this design with UniQ, Atlas, and Lazy Qubit Reordering. One visible backend difference is target-axis permutation. CUDA uses cuTensor, whereas the evaluated ROCm 7.2.3 stack requires a custom complex-valued HIP kernel because hiptensorPermute returns NOT_SUPPORTED for the required datatype. GEMM precision, sampling-kernel choice, and MPI transport likewise vary below the common execution path.

## V. EVALUATION

The evaluation follows the architecture across three changes of regime: semantic routing between tableau and full-state execution, the fused-operator contract across NVIDIA and AMD backends, and explicit layout from local to distributed execution. Tab. II collects the main results; the subsections that follow give the workloads and measurement details. a) Sampling: For a state |ψ⟩= P2N−1 x=0 ψx |x⟩, state- vector engines draw computational-basis outcome x with probability p(x) = |ψx|2 through a hierarchical cumulative distribution function (CDF). CUDA, HIP, CPU, and MPI paths use different block decompositions, but retain block totals and inter-rank offsets in FP64; the MPI-GPU within-block prefixes remain FP32 to bound memory. Inverse-CDF search uses a strict upper bound, which prevents selection of a zero-probability bin. Appendix B records the concrete decompositions and numerical precision used in the evaluated builds. A. Experimental setup a) Platforms: Ella is a Pawsey GH200 system with one Grace CPU and one Hopper GPU per node, ConnectX-7 Table I. Representative lowerings and primitives for the full-state path. Backends preserve fused-operator semantics and logical layout but may choose different specialised kernels, libraries, arithmetic modes, and transports. Operation CUDA / NVIDIA HIP / AMD CPU Target-axis permutation cuTensor permutation Custom complex-valued permutation kernel; the evaluated ROCm stack lacks the required hipTensor datatype path

## Host permutation

Fused update Diagonal/controlled kernels, small-width kernels, or cuBLAS fallback Diagonal/controlled kernels, custom dense kernels, or hipBLAS/hipBLASLt fallback In-place diagonal and small-width kernels; host permutation with OpenBLAS or NVPL GEMM fallback

## Diagonal updates and
sampling

Device kernels; single-GPU two-level sampler with 210-entry blocks and FP64 block totals Device kernels; the same single-GPU sampler Host loops; FP64 cumulative distribution with 216-entry blocks in the local CPU state-vector engine Distributed bit transpose XOR-partner and coalesced exchanges through HPC-X OpenMPI/UCX XOR-partner and coalesced exchanges through Cray MPICH and its Heterogeneous System Architecture (HSA) GPU Transfer Library (GTL) First-free placement with pairwise or full-permutation MPI lowerings Table II. Summary of the reported intervals (baseline short names: §V-A). Appendix B defines the timing and repetition policies.

## Target
Primary baseline
Main result

Synthetic routing Stim At N = 30, routing cuts QSIMADV’s noisy-GHZ interval from 454.8 s to 5.8 ms and the tableau reaches N = 1000; Stim stays ahead in every cell Ella single device AER-noCuQ On random brickwork, QSIMADV records the lowest Hopper interval at N = 30 and 32 and the lowest Grace FP64 interval at N = 28 and 30; qsim-CPU is lower on all four Grace cells Setonix single device AER-GPU On random brickwork, QSIMADV leads the four-engine MI250X comparison at N = 24–30; on EPYC it leads the FP64 engines at N = 24, while AER leads FP64 at N = 26–30 and qsim-CPU leads overall Ella strong scaling AER-noCuQ AER-noCuQ takes the GPU lead from P = 4, and QuEST the CPU lead from P = 4 Setonix strong scaling AER-GPU QSIMADV is ahead of both comparators at every measured GPU and CPU rank 256-rank GPU weak scaling AER-GPU Both reach N = 38 (2 TiB); QSIMADV leads through P = 32 and AER from P = 64 256-rank CPU weak scaling AER-CPU QSIMADV reaches N = 36 (1 TiB), stays ahead of AER throughout, and leads QuEST from P = 4 (measured to P = 128) RoCEv2, and HPC-X OpenMPI. Setonix combines MI250X GPU nodes with HPE Slingshot 11 and Cray MPICH; each node exposes eight GPU compute dies (GCDs). Its CPU-only partition uses EPYC 7763 nodes. Appendix A records the hardware and software environment. 2,410–3,010 at N = 24, 28, and 30), so per-size readings at N = 26 partly reflect the instance. Synthetic GHZ and random-Clifford circuits exercise semantic routing. Application tests cover the quantum approximate optimisation algorithm (QAOA) [28], unitary coupled-cluster singles-and-doubles (UCCSD)-style Pauli rotations [29], and QASMBench [30]. b) Baselines: The state-vector comparison includes Qiskit AER [2], qsim [5], Qulacs [12], PennyLane LIGHTNING [4], and QuEST [13]; the Clifford experiments add AER’s sta- biliser method and Stim [17]. AER-noCuQ is AER’s open GPU backend without CUSTATEVEC, AER-cuQ links AER to CUSTATEVEC, and AER-GPU on Setonix is the ROCm/HIP build. d) Comparison protocol: Except for the application study, the cross-tool figures use each simulator’s internal execution interval. QSIMADV includes gate execution and sampling; AER times .run() after construction; QuEST times its gate-evolution loop, excluding environment setup and sampling, and every QuEST interval below uses that boundary. The application study uses its separately predefined runner boundaries (§V-E). The paired Ella single-GPU and Setonix single-GCD comparisons use one exclusive allocation, vary the tool order between repetitions, and report five-run medians. Table captions identify medians and individual observations for the other series; Appendix B consolidates the timing, shot- count, repetition, and reduction policies. All tools receive the same raw OpenQASM 3.0 inputs without external transpilation. AER, LIGHTNING, and qsim parse them through framework front ends, Qulacs uses its converter, and QuEST uses the benchmark bridge. The qsim-GPU rows report the tool-native sampled-run interval, including state download and sampling. c) Workloads: State-vector sweeps use 50-layer ran- dom brickwork, with alternating layers of random single- qubit rotations and nearest-neighbour two-qubit gates, rand{24,26,28,30,32}x50, and the quantum Fourier transform (QFT), qft{24,26,28,30,32}. The Setonix paired sweep additionally covers N = 18–22 below the range shared by all four GPU tools. The MI250X sweep ends at N = 30; rand32x50 is reported separately as a managed- memory capacity case. The audited gate counts also show rand26x50 to be a lighter instance (1,951 operations against B. Semantic routing on synthetic Clifford workloads Clifford circuits can use a polynomial-size tableau instead of an exponential state vector. We submit noisy GHZ and random-Clifford inputs through the generic gpu entry point and test whether QSIMADV routes them before GPU allocation. The GHZ sweep attaches depolarising noise with pdep = 10−2 after the Hadamard and each CX gate, while the random- Clifford inputs are noiseless. Using one common rate is a remains 6% behind the CUSTATEVEC-backed AER-cuQ path. It records the lowest interval in the panel at N = 30 and 32: the AER-noCuQ-to-QSIMADV ratios are 1.98 and 2.19, and the AER-cuQ-to-QSIMADV ratios are 1.84 and 2.00. QuEST-GPU, the next non-AER baseline at these sizes, takes 6.8 and 7.3 times the QSIMADV interval. The five paired observations in each QSIMADV/AER cell span at most 2.2% of the median, except at N = 24, where the span is 3.9%.

## Semantic routing

Stim Aer (stabiliser) qsimadv SV (forced) qsimadv (tableau) dotted: random Clifford beyond single-GPU SV reported interval (s) 78,000× The Hopper evaluation also includes a fusion-cap sweep on rand32x50, the largest random circuit. Wider fusion reduces the number of full-state updates, but increases the fused-matrix size and construction cost. One run at each of Kmax = 5, 6, 7 gives intervals of 12.35, 8.51, and 7.39 s, respectively. At this state size the sweep favours Kmax = 7, the value used by the CUDA path; Appendix C-B gives the full size-dependent and backend rules. 102 103 qubits N The precision bridge in Tab. VIII adds a separate FP64 AER sweep to the paired Hopper data. Its AER-to-QSIMADV time ratios on rand24x50, rand26x50, and rand28x50 are 0.49, 1.01, and 1.89, respectively. b) Grace CPU: Fig. 4(b) and Tab. IX show a second large- N crossover. QSIMADV trails AER at N = 24 and 26, overtakes it at N = 28, and retains the lowest FP64 interval at N = 30. LIGHTNING, Qulacs, and QuEST are slower than QSIMADV at all four plotted widths. The mixed-precision ordering is different: qsim-CPU uses FP32 and records the lowest interval in every cell. Fig. 3. Synthetic Clifford routing on Ella. Colour and marker shape identify the simulator; solid curves show noisy GHZ inputs and dotted curves show random-Clifford inputs. The forced single-GPU state-vector path ends at N = 30, whereas automatic tableau routing reaches N = 1000. Values are listed in Tab. XVII. benchmark choice rather than a routing requirement: supported Pauli-channel parameters may vary by circuit location. Timings are reported on Ella; the routing decision depends on the circuit, noise model, and requested output semantics rather than the hardware backend. c) MI250X GPU: Fig. 5(a) and Tab. IV show AER-GPU ahead at N = 18–22, followed by a crossover between N = 22 and 24. From N = 24 through 30, QSIMADV records the lowest interval among all four GPU engines. The AER-to-QSIMADV ratio ranges from 1.15 to 2.16 over those shared sizes. At N = 30, QuEST-GPU takes 4.2 times the QSIMADV interval and the FP64 LIGHTNING-KOKKOS path takes 16.6 times the interval. Semantic routing changes the feasible problem size rather than merely shortening a fixed run (Fig. 3). The forced single- GPU state-vector path ends at N = 30, where it already takes 454.8 s. The same generic gpu request takes 5.8 ms after routing and proceeds to N = 1000 in 0.884 s, far beyond the memory limit of full-state allocation, without requiring the user to choose a stabiliser engine. The routed path also overtakes AER’s stabiliser sampler beyond approximately 200 qubits on GHZ and at both random-Clifford sizes, by up to 2.7× on randcliff500. Stim remains fastest in every measured cell. d) EPYC CPU: Fig. 5(b) and Tab. XII complete the four- backend comparison. At N = 24, QSIMADV has the lowest FP64 interval. AER takes the FP64 lead from N = 26 and is 1.38× faster than QSIMADV at N = 30. Qulacs also edges QSIMADV at N = 26, but QSIMADV is the next FP64 engine after AER at N = 28 and 30 and remains ahead of QuEST at every plotted width. As on Grace, the FP32 qsim-CPU path records the lowest interval throughout the panel. As an output sanity check, the three tools’ all-zero GHZ frequencies differ by at most 1.02 independent-binomial standard errors. This is one sampled marginal, not complete state fidelity or a check of the random-Clifford distributions (Appendix C-H). Across both GPU vendors, the crossover moves in QSI- MADV’s favour as N grows, and QSIMADV leads the larger random-brickwork cells. The CPU ordering is less uniform: QSIMADV gains the large-N FP64 lead on Grace but not on EPYC, while the FP32 qsim-CPU path remains fastest on both. C. The fused-operator interface across backends The same fused-operator contract drives four backends: Hopper GPU and Grace CPU on Ella, and MI250X GPU and EPYC CPU on Setonix. Each backend supplies its own permutation, matrix, and structured-gate kernels (Tab. I). The random-brickwork comparisons occupy panels (a) and (b) of Figs. 4 and 5; the strong-scaling panels (c) and (d) are analysed separately in §V-H. QFT results follow in §V-D. D. Diagonal-heavy QFT QFT interleaves Hadamards with controlled phases and favours element-wise diagonal updates over generic TTGT. Fig. 8 plots the N ≥24 range shared across the systems. On Hopper, the crossing sits one sweep step above the random- brickwork one: the AER-noCuQ-to-QSIMADV time ratio rises from 0.09 at qft24 to 1.31 at qft30 and 1.44 at qft32, while the corresponding AER-cuQ ratios are 0.93 and 1.02 at a) Hopper GPU: Fig. 4(a) and Tab. III compare seven GPU paths. At N = 24 and 26, both AER variants are faster than QSIMADV, while QSIMADV is already ahead of qsim- GPU, LIGHTNING-GPU, Qulacs-GPU, and QuEST-GPU. At N = 28, QSIMADV overtakes the open AER-noCuQ path but (a) Hopper GPU · single-node (rand-N) (b) Grace CPU · single-node (rand-N) qsim (Google) Lightning Qulacs QuEST-GPU

## Qulacs
QuEST-CPU
qsimadv

reported interval (s) reported interval (s) N=32: Aer-noCuQ/qsimadv = 2.19× N=30: Aer/qsimadv = 1.11× 24 26 28 30 32 qubits N 24 25 26 27 28 29 30 qubits N (c) GPU-MPI · rand30x50 strong scaling (d) CPU-MPI · rand30x50 strong scaling qsimadv T(8)/T(1) = 2.19 qsimadv T(8)/T(1) = 1.72 reported interval (s) reported interval (s) qsimadv ideal T(1)/P Aer-noCuQ Aer-cuQ qsimadv qsimadv ideal T(1)/P Aer QuEST-CPU qsimadv 1 2 4 8 ranks P (one GPU or node each) 1 2 4 8 ranks P (one GPU or node each) Fig. 4. Ella execution intervals: random-brickwork single-node results on (a) Hopper GPU and (b) Grace CPU, followed by rand30x50 strong scaling on (c) GPU-MPI and (d) CPU-MPI. Here T(P) denotes QSIMADV’s interval on P ranks; the dotted T(1)/P reference is ideal strong scaling. LIGHTNING-GPU and Qulacs-GPU use FP64; other GPU series use FP32. Table III. Single-GPU intervals on Ella. Values are intervals in seconds; bold marks the lowest available value in each row. The three leftmost columns are five-run paired medians; the other four are single observations. “T/O” denotes timeout; “OOM” marks Qulacs-GPU cells whose two-buffer footprint exceeds device memory. Circuit QSIMADV AER-noCuQ AER-cuQ LIGHTNING-GPU qsim-GPU Qulacs-GPU QuEST-GPU rand24x50 0.256 0.087 0.084 1.095 2.581 0.437 0.393 rand26x50 0.410 0.238 0.222 2.825 10.04 1.409 0.751 rand28x50 0.901 0.949 0.850 16.72 40.06 7.565 3.506 rand30x50 1.932 3.821 3.554 67.82 160.0 30.96 13.10 rand32x50 7.415 16.27 14.85 290.4 T/O OOM 54.40 qft24 0.231 0.020 0.018 0.054 2.479 0.132 0.046 qft26 0.267 0.068 0.052 0.173 9.885 0.552 0.104 qft28 0.419 0.277 0.201 0.668 39.52 2.255 0.347 qft30 0.930 1.221 0.861 2.853 158.1 9.510 1.338 qft32 3.744 5.398 3.817 12.62 T/O OOM 5.705 the two larger sizes. On MI250X, AER-GPU is 2.9× and 1.4× faster at N = 24 and N = 26, and QSIMADV takes the lower median at both larger sizes, where the paired medians differ by only 1.7% and 2.8%. AER has the lowest FP64 time on every CPU QFT cell of both machines. (a) MI250X · one GCD (rand-N) (b) EPYC CPU · one node (rand-N)

## Qulacs
QuEST-CPU
qsimadv

reported interval (s) reported interval (s) N=30: Aer-GPU/qsimadv = 2.16× N=30: qsimadv/Aer = 1.38× 24 25 26 27 28 29 30 qubits N 24 25 26 27 28 29 30 qubits N (c) GPU-MPI · rand30x50 strong scaling (d) CPU-MPI · rand30x50 strong scaling qsimadv T(8)/T(1) = 0.30 qsimadv T(8)/T(1) = 0.35 reported interval (s) reported interval (s) qsimadv ideal T(1)/P Aer-GPU QuEST-GPU qsimadv qsimadv ideal T(1)/P Aer QuEST-CPU qsimadv 1 2 4 8 ranks P (one GPU or node each) 1 2 4 8 ranks P (one GPU or node each) Fig. 5. Setonix execution intervals: random-brickwork results on (a) one MI250X GCD and (b) one EPYC CPU node, followed by rand30x50 strong scaling on (c) GPU-MPI and (d) CPU-MPI. Here T(P) denotes QSIMADV’s interval on P ranks; the dotted T(1)/P reference is ideal strong scaling. LIGHTNING-KOKKOS uses FP64; other GPU series use FP32. E. Application circuits Table IV. Single-GCD intervals on Setonix. Values are intervals in seconds; bold marks the lowest available value in each row. QSIMADV and AER-GPU are five-run paired medians; QuEST-GPU and LIGHTNING-KOKKOS are single observations. LIGHTNING-KOKKOS uses FP64 and the other columns FP32. Rows below N = 24 were not measured by every tool; “N/M” denotes an unmeasured cell. The non-Clifford application inputs exercise the full-state R2 path, while the Clifford qec9xz_n17 input exercises R1 on a public benchmark. The sweep reports five-run medians under its predefined runner policy: QSIMADV is timed over its whole process, while AER and LIGHTNING are timed after construction (Appendix B). The QSIMADV interval therefore includes stages excluded from the two baseline intervals. LIGHTNING or AER reports the lower interval on the smaller non-Clifford QAOA, UCCSD-style, and QASMBench inputs. At N = 30 the QAOA ordering changes with depth: AER is lower at one layer on both topologies and at two layers on the ring, while QSIMADV is lower at three layers on both and at two layers on the 3-regular graph. On qec9xz_n17, the routed QSIMADV interval is lower than both GPU state-vector baseline intervals (Tab. XVIII).

## Circuit
QSIMADV
AER-GPU
QuEST-GPU
LIGHTNING-KOKKOS

rand18x50 0.093 0.027 N/M 0.249 rand20x50 0.098 0.038 N/M 0.317 rand22x50 0.121 0.080 N/M 0.565 rand24x50 0.195 0.254 0.502 2.259 rand26x50 0.901 1.040 1.406 5.562 rand28x50 2.131 4.130 8.302 33.82 rand30x50 8.283 17.86 34.56 137.8 qft18 0.081 0.006 N/M 0.033 qft20 0.082 0.007 N/M 0.039 qft22 0.086 0.013 N/M 0.055 qft24 0.105 0.037 0.046 0.104 qft26 0.194 0.140 0.178 0.311 qft28 0.594 0.604 0.776 1.225 qft30 2.576 2.649 3.488 5.244 The rand32x50 capacity cell is discussed in Appendix C-F. F. Backend costs MPI weak scaling (qsimadv) On GH200, an external device trace shows compara- ble cuTensor-permutation and cuBLAS-GEMM time in rand32x20. On MI250X, rand32x50 pages through man- aged memory and takes 627 s against 148 s for the matched- FP32 QuEST-GPU build; QSIMADV’s two-buffer working set does not fit in GPU memory, whereas QuEST’s in-place state does. The built-in fastTranspose host region covers 91% of the profiled interval, but its stream synchronisation includes preceding asynchronous work, so the value bounds permutation time rather than attributing it to a kernel. Appendix C-F gives the memory accounting and profiler scope for both systems. ideal weak (flat) Ella Hopper GPU (Qloc = 31) Setonix MI250X GPU (Qloc = 28) Ella Grace CPU (Qloc = 28) Setonix EPYC CPU (Qloc = 28) relative interval T(P)/T(1) Setonix GPU T(8)/T(1) = 2.45 G. Placement policy The placement comparison replays the 141-entry fused stream of rand30x50 at P ∈{2, 4, 8} (Qloc = 29, 28, 27, respectively). With the identity initial layout at P = 8, first- free placement requires 98 promotions, compared with 42 under all-frame farthest-next-use. A second replay uses the production initial layout and victim restriction. On this stream, the bounded production planner and its full-future counterpart both require 11, 26, and 39 promotions across P ∈{2, 4, 8}; allowing all Qloc local positions as victims instead gives 11, 23, and 38. Thus the bounded planner matches the choices of the full-future replay within the production victim set for this stream. The remaining difference from the corollary’s all-frame model comes from the restricted victim set. 1 2 4 8 ranks P (constant per-rank work) Fig. 6. QSIMADV small-rank MPI weak scaling on 50-layer random brickwork, normalised as T(P)/T(1), where T(P) is the interval on P ranks. Solid series hold Qloc = 28 on Setonix GPU and both CPUs, the dashed Ella GPU series holds Qloc = 31, and the dotted line is ideal. The Ella Grace CPU series rises fastest, to 14× at P = 8. Tab. XIV), QSIMADV leads both competitors at every rank and improves from 245.46 to 86.83 s, a 2.8× speedup. It stays 14–17% below QuEST at P = 2–8, while AER takes 4.8–7.8 times the QSIMADV interval. The platform split is therefore consistent across processor types: from one to eight ranks, QSIMADV obtains a net speedup on both Setonix paths, whereas both Ella paths end slower than their one-rank runs and cede the lead at P = 4. H. Distributed execution For strong scaling, the circuit remains rand30x50 while the number of ranks P increases; panels (c) and (d) of Figs. 4 and 5 show the GPU and CPU results. Weak scaling instead holds the rank-local state size fixed and adds one qubit whenever P doubles; Figs. 6 and 7 show the small-rank and large-scale results. Both experiments include local execution, placement, transport, and MPI overhead. c) GPU weak scaling: The qsim-only small-rank com- parison in Fig. 6 separates the two systems: at P = 8, the normalised interval is 2.45× its one-rank value on Setonix and 11.8× on Ella. The larger Setonix comparison in Fig. 7(a) and Tab. XV reaches 256 ranks with Qloc = 30. QSIMADV leads AER-GPU from P = 8 through 32; the ordering reverses between P = 32 and 64, and AER-GPU remains ahead through P = 256. Both engines reach N = 38, corresponding to a 2 TiB FP32 state. From P = 32 to 256, the QSIMADV interval grows by 52%. a) GPU strong scaling: On Ella (Fig. 4(c) and Tab. X), QSIMADV has the lowest interval at P = 1 and 2, then AER- noCuQ takes the lead at P = 4 and 8. The QSIMADV interval rises from 1.97 s on one rank to 4.32 s on eight, so additional GH200 nodes do not provide a net speedup for this fixed problem; at P = 8, however, AER-noCuQ is ahead by only 5%. The CUSTATEVEC-backed AER series is second at P = 1, has no completed P = 2 cell, leads QSIMADV at P = 4, and trails it at P = 8. d) CPU weak scaling: In Fig. 6, the QSIMADV interval at P = 8 is 3.0× its one-rank value on Setonix and 14.0× on Ella. Fig. 7(b) and Tab. XVI extend the Setonix comparison at Qloc = 28. QuEST is narrowly ahead at P = 2, taking 38.63 s versus QSIMADV’s 39.30 s; QSIMADV takes the lead at P = 4 and retains it through the last measured QuEST cell at P = 128. QSIMADV is ahead of AER throughout, with the AER-to-QSIMADV ratio narrowing from 8.5 at P = 2 to 3.4 at P = 256. At that final rank count, QSIMADV reaches N = 36, a 1 TiB FP64 state. The Setonix result is qualitatively different (Fig. 5(c) and Tab. XIII). All three engines improve from one to eight ranks, and QSIMADV is fastest at every rank. Its interval falls from 8.63 to 2.55 s, a 3.4× strong-scaling speedup. Across the four rank counts, AER-GPU takes 1.8–2.1 times the QSIMADV interval and QuEST-GPU takes 4.0–6.8 times the interval. b) CPU strong scaling: On Ella (Fig. 4(d) and Tab. XI),

## VI. RELATED WORK

QSIMADV leads at P = 1 and 2, but QuEST crosses below it at P = 4. By P = 8, QuEST takes 88.15 s against QSIMADV’s 151.28 s. AER is slower than QSIMADV at every completed rank, by factors from 3.3 to 10.4. On Setonix (Fig. 5(d) and We compare QSIMADV along two axes: when a simulator selects its representation and where vendor-specific code enters the state-vector path. directly to CUDA, HIP, and CPU primitives, providing a comparison between framework-level and simulator-interface portability. (a) MI250X GPU · Qloc = 30 · FP32 33 34 35 36 37 38 qubits N c) qsim (Google): Google’s Schr¨odinger-style qsim simu- lator [5] provides native CUDA and HIP state-vector backends, a C++ MPS simulator, an optional CUQUANTUM path, and the hybrid Schr¨odinger–Feynman qsimh engine [20]. The companion qFlex work [31] provides the tensor-network precedent for QSIMADV’s cutn engine. reported interval (s) N=38 · 2 TiB qsimadv ideal from P=8 Aer-GPU qsimadv d) JUQCS: De Raedt et al. introduced a distributed simulator in 2007 [32]. JUQCS-E later used exact complex am- plitudes, while JUQCS-A used an adaptive polar encoding to reach 48-qubit Shor simulations on Sunway TaihuLight [14]. Its high-bit/low-bit state partition is a predecessor of QSIMADV’s explicit distributed layout. 8 16 32 64 128 256 MPI ranks P (b) EPYC CPU · Qloc = 28 · FP64 29 30 31 32 33 34 35 36 qubits N reported interval (s) e) Multi-GPU systems: Communication-avoiding state- vector simulation spans the H¨aner–Steiger CPU implemen- tation [26], HyQuas [27], UniQ [33], NWQ-SIM [15], and Atlas [16]. HyQuas includes a transpose-then-GEMM strategy, and Atlas re-stages logical-to-physical mappings between subcircuits with an integer-linear-programming partitioner. QSIMADV retains each resulting permutation in a per-gate layout map and implements the same contract on CUDA and HIP. qsimadv ideal from P=2 Aer

## QuEST-CPU
qsimadv

N=36 · 1 TiB 2 4 8 16 32 64 128 256 MPI ranks P Lazy Qubit Reordering [34] also defers cross-rank movement through time-space tiling. QSIMADV contributes a place- ment invariant and an optimal promotion count for its ideal demand model; its production policy remains a bounded- lookahead heuristic. Q2Chemistry [35] adds buffered overlap and dependency-aware contraction on CUDA/NCCL. Fig. 7. Setonix weak scaling to 256 ranks on depth-20 random brickwork: (a) GPU at Qloc = 30 and FP32 reaches N = 38 (2 TiB); (b) CPU at Qloc = 28 and FP64 reaches N = 36 (1 TiB). A. NVIDIA CUQUANTUM and its consumers The NVIDIA CUQUANTUM SDK provides CUSTATEVEC for state-vector operations and CUTENSORNET for tensor-network contraction [1]. AER’s GPU backend [3], LIGHTNING-GPU [4], qsim-GPU [5], and CUDA-Q [6] use CUSTATEVEC for their fastest NVIDIA state-vector paths. The SDK delivers high throughput on NVIDIA systems but does not define an AMD backend. Tab. V compares execution modes, automatic stabiliser routing, and direct or framework-mediated HIP support. Brown et al. [6] study CUQUANTUM scaling across PCIe, NVLink, multi-node NVLink, and InfiniBand GPUDirect re- mote direct memory access (RDMA). They also discuss CUDA- to-HIP changes for inter-process communication, but report no AMD measurements. Our AMD implementation therefore starts below the quantum-library layer: ROCm supplies matrix, permutation, and communication primitives, while QSIMADV supplies the gate schedule and layout semantics. B. Open-source state-vector simulators a) AER: AER is the main multi-representation baseline within Qiskit [2]. Its default automatic method may select a stabiliser engine [3]; an explicit state-vector/GPU method does not. QSIMADV extends inspection to eligible sampled- count requests made through its generic dense and gpu entry points. AER uses Monte-Carlo trajectories for non-Clifford noise, while QSIMADV also includes an exact density-matrix path. b) LIGHTNING: PennyLane LIGHTNING [4] supports NVIDIA and AMD GPUs through native CUDA and Kokkos- based paths. QSIMADV instead maps its tensor-axis interface Table V. Feature comparison across selected open-source quantum simulators. Single-GPU denotes a non-distributed GPU backend. ✓= supported; ∼= supported through a portability framework or compiler vectorisation; ◦= path-dependent; NV = NVIDIA-only; × = not supported. Simulator MPI Multi- SIMD Single- HIP direct OpenQASM 3.0 Stab. Density MPS Tensor- GPU CPU GPU HIP native auto-route matrix network AER [3] ✓ ✓ ✓ ✓ ✓ ✓ via Qiskit ◦ ✓ ✓ NV LIGHTNING [4] ✓ ✓ ✓ ✓ ∼ × via PennyLane × × NV NV qsim [5], [20] × ◦ ✓ ✓ ✓ ✓ via Cirq × × ✓ × JUQCS [14] ✓ × ✓ × × × via translator × × × × NWQ-Sim (SV-/DM-Sim) [15], [36], [37] ✓ ✓ ✓ ✓ ✓ ✓ ✓ × ✓ × × Atlas [16] ✓ ✓ × ✓ × × ✓ × × × × Q2Chemistry [35] ✓ ✓ ✓ ✓ × × × × × × × Qulacs [12] ◦ × ✓ ✓ × × ◦ × ✓ × × QuEST [13] ✓ ✓ ✓ ✓ ✓ ✓ × × ✓ × × Intel-QS [25] ✓ × ✓ × × × × × × × × mpiQulacs [38] ✓ × ✓ × × × × × × × × ProjectQ [39] × × ✓ × × × × × × × × MindQuantum [40] × × ✓ ✓ × × ◦ × ✓ ✓ × Yao.jl [41] × × ∼ ✓ × × × × ✓ × × QSIMADV (this work) ✓ ✓ ✓ ✓ ✓ ✓ ✓ ✓ ✓ ✓ NV ◦AER Stab.: auto-selects stabilizer only under the default method=automatic; an explicit state-vector/GPU method suppresses it. ◦qsim MultiGPU: only via NVIDIA CUSTATEVEC (CUQUANTUM); the native CUDA path is single-device. ◦Qulacs MPI: dedicated USE_MPI build only; power-of- two ranks, no measurement, incompatible with the GPU backend. ◦Qulacs/MindQuantum OpenQASM 3.0: in-tree converter (qulacs.converter / mindquantum.io.OpenQASM) for a limited gate subset, not a full parser. LIGHTNING HIP (∼): AMD-GPU execution is provided by LIGHTNING-KOKKOS through the Kokkos HIP backend; LIGHTNING-GPU is NVIDIA-only. Yao SIMD (∼): CPU vectorisation is supplied by the Julia/LLVM compiler rather than hand-written intrinsic kernels. JUQCS: the row reflects the cited CPU-only JUQCS-E/A [14]; the later JUQCS-G line adds NVIDIA multi-GPU. the QSIMADV interval at N = 30. Because LIGHTNING- KOKKOS uses FP64 and differs from QSIMADV above the kernel layer, the figure does not isolate Kokkos, but it does report the performance of the production portable path evaluated here. Within QSIMADV, the Hopper fusion sweep gives complementary evidence: changing the backend parameter Kmax from 5 to 7 yields a 1.67× speedup (§V-C). Together, the system comparison and the in-system sweep motivate retaining direct control over each lowering. C. Backend portability and native specialisation High-performance-computing portability frameworks such as Kokkos [42], OpenMP target offload [43], and SYCL [44] com- pile a common kernel source for multiple targets; LIGHTNING- KOKKOS is a production quantum-simulation example [4]. These approaches reduce the cost of supporting new devices. Our priority is per-platform performance on the supported target families. A permutation or dense update streams the full 2N- amplitude state, so a suboptimal lowering repeats its cost across every amplitude. We therefore do not place a common-source kernel model at the primary backend boundary.

## VII. DISCUSSION

Semantic routing produces the largest change in the experi- ments because it replaces exponential state evolution with a polynomial tableau. R1 lets a sampled-count request through a generic full-state entry point reach the tableau path without an explicit representation choice. Once a circuit stays on the state- vector path, fusion, permutation, matrix arithmetic, sampling, and communication all contribute to the reported execution interval. Instead, QSIMADV makes the simulator interface portable and the lowering backend-specific. R2 and R3 fix the ordered-target, operator, and layout semantics, while each backend selects its native data movement, matrix library, arithmetic mode, fusion width, and transport. CUDA uses cuTensor for permutation; HIP uses a custom kernel because the evaluated hipTensor path does not support the required datatype; CPUs use their own direct kernels and NVPL or OpenBLAS. The small macro layer only maps operation names to vendor symbols at compile time. A new backend must implement the same contract, but need not inherit another backend’s algorithmic choices. This accepts more backend code and validation in exchange for direct control of hardware-sensitive operations. Both single-GPU random-brickwork sweeps exhibit a crossover: AER is faster at smaller widths, whereas QSIMADV leads at larger widths, with the crossing occurring at different widths on the two systems. The measurements establish the crossover but do not isolate its component-level cause. Strong scaling likewise changes the ordering across fabrics: QSIMADV holds the GPU lead at every Setonix rank but cedes it from P = 4 on Ella despite using the same logical layout scheme. The CPU implementation follows the tiered instruction-set fallback pattern familiar from BLIS and OpenBLAS [45]. State- vector direct kernels use NEON or AVX2 specialisations where available and otherwise rely on compiler-vectorised or scalar code. The tableau groups 64 row values in each unsigned integer and dispatches its bitwise XOR, XOR–AND, and XOR– AND-NOT operations across AVX-512, AVX2, SVE/SVE2, NEON, and scalar implementations; these operations follow Aaronson–Gottesman’s analysis of tableau updates [11]. A. Model and measurement limits Within Corollary 1’s all-frame demand model, farthest-next- use minimises promotions for a fixed fused stream and initial layout, uniform costs, full future knowledge, and no prefetch. Production GPU-MPI instead uses bounded lookahead and a restricted victim set, while CPU-MPI uses first-free placement. The corollary is therefore a reference count, not a wall- time model; the measured MPI intervals cover the complete The evaluation shows why this control matters. In Fig. 5(a), the LIGHTNING-KOKKOS path is above the three FP32 paths at every shared random-brickwork width and takes 16.6 times edge backend can use the same upper layers with compact SIMD or accelerator kernels, and can avoid an exponential allocation whenever circuit semantics permit a smaller represen- tation. Porting becomes a backend integration task rather than a rewrite of the simulator. More broadly, vendor independence need not require generic execution: it can be established by stable semantic contracts above the hardware boundary while performance-critical execution remains native below it. distributed path, including transport and local kernels, with no standalone timing ablation for the layout map. Deterministic state comparisons under strict FP32 remain future work. For the MI250X rand32x50 capacity case, an in-place single-buffer lowering would reduce the working set; separating permutation, dense updates, synchronisation, and paging would require HIP events or a device profile. The GPU-MPI ordering across the two fabrics motivates evaluating a specialised pairwise bit swap and topology-aware rank- bit assignment, neither of which is isolated by the current experiments. B. Architectural lessons The architecture exposes decisions that simulators often hide inside an engine factory. Semantic routing is placed after circuit, noise, and output inspection but before state allocation, where it can change the representation without changing the request. Layout is stored as part of the simulator state, so local kernels and MPI dispatch can retain non-canonical orders without redefining logical qubits. The backend boundary is deliberately small: upper layers specify ordered targets, operators, and layouts, while native kernels, arithmetic modes, and transports remain backend choices. Together, semantic routing reaches the tableau path, while the operator and layout contracts span vendor-native full-state and distributed execution.

## VIII. CONCLUSION

QSIMADV treats simulator portability as controlled late binding, rather than a single implementation path imposed on every machine. Circuit, noise, and output semantics select the representation before memory is committed; ordered fused operators bind to concrete kernels only after their physical targets are known; the logical layout binds to data movement only when an operation demands non-local state. These contracts preserve circuit semantics while allowing representation, arithmetic, permutation, and transport to be selected independently where the required information becomes available. The resulting architecture spans a polynomial tableau and exponential full-state execution, NVIDIA and AMD GPUs, Arm and x86 CPUs, and local and MPI-distributed memory without collapsing them into one lowest-common-denominator kernel. A backend implements a compact contract for matrix multiplication, permutation, and sampling, with exchange added for distributed execution. Dense arithmetic can therefore be delegated to mature, production-tested libraries such as cuBLAS, hipBLAS, NVPL, and OpenBLAS, while native kernels cover workloads that need a more specialised treatment. The single-device crossovers at larger problem sizes and the 256-rank runs show that the shared interface is not merely portable: it can retain competitive performance while exposing platform-specific capabilities. The same separation provides a practical route to hardware beyond the systems evaluated here. A new HPC backend can reuse semantic routing, fusion, and logical layout, then bind the primitive contract to its matrix library, tensor operations, instruction set, and communication fabric. An embedded or

## APPENDIX A
ARTEFACT AND EXPERIMENTAL ENVIRONMENT

the aggregate CSVs and provenance records used by the paper; the authors retain the scheduler logs. The measurements can be rerun on Ella and Setonix or comparable HPC systems, although exact timings remain system-dependent. Appendix B defines the timing, repetition, and numerical protocols. The simulator and cluster recipes are hosted at https: //github.com/Quleaf/qsimadv. In the simulator repository, in- puts and build/run recipes are under data/input/ and tests/benchmark/; the manuscript repository contains the source CSVs under data/ and plotting scripts under scripts/. data/README.md maps each result family to its source files and campaign provenance and documents figure regeneration.

## Timing and repetitions

QSIMADV’s compute_time_s includes gate execution, distributed exchange, and terminal sampling; it excludes process launch, parsing, fusion-plan construction, and result output. AER times .run() after circuit parsing and simulator construction, with a warm call where noted. LIGHTNING times QNode execution after construction. QuEST times its gate- evolution loop and excludes environment/register creation, initialisation, and sampling. Cross-tool ratios compare these internal intervals rather than a common end-to-end timer.

## Platforms

a) Ella (NVIDIA Grace Hopper GH200): Each node contains one GH200 Superchip with a 72-core Grace ARM CPU, a Hopper GPU with 96 GiB of third-generation high- bandwidth memory (HBM3), and coherent NVLink-C2C memory access. Inter-node communication uses dual ConnectX- 7 interfaces over RoCEv2; the evaluated MPI path uses HPC-X OpenMPI over UCX with GPUDirect RDMA. Strong-scaling runs assign one node per rank. The application study retains its predefined runner bound- aries: QSIMADV is measured by process wall time, including launch, parsing, fusion planning, execution, sampling, and output, whereas AER and LIGHTNING use the post-construction intervals above. The application table therefore compares medians under a tool-native timing policy. b) Setonix (AMD MI250X with HPE Slingshot): Each GPU node combines one 64-core EPYC 7A53 host with four MI250X cards, exposed as eight logical GPU compute dies (GCDs), each with 64 GiB HBM2e. Inter-node communication uses HPE Slingshot 11 and the libfabric CXI provider. The evaluated MPI path uses Cray MPICH 8.1.32; GPU-aware transfers use its HSA GPU Transfer Library (GTL). Unless a caption states otherwise, each table cell is one timed observation. The paired Ella single-GPU and Setonix single-GCD sweeps, the application workloads, the QSIMADV GPU-MPI strong-scaling series, and the Setonix GPU small- rank curve at Qloc = 28 report five-run medians; the paired sweeps alternate tool order. The QSIMADV and QuEST 256- rank weak-scaling series report three-run medians, with each QuEST run first reduced to the maximum time across ranks. Timeouts are marked and excluded from summaries. c) Setonix CPU-only partition: The single-node CPU and CPU-MPI experiments use AMD EPYC 7763 “Milan” nodes from the work partition. Each node presents 128 physical cores across eight non-uniform memory access domains; the experiments assign 64 OpenMP threads per rank. These results are reported separately from those obtained on the EPYC 7A53 “Trento” hosts of the GPU nodes. Most runs use S = 1000 shots. The Setonix Qloc = 28 GPU weak-scaling series, the QSIMADV side of both 256- rank weak-scaling sweeps, and the application study use S = 100; AER retains S = 1000 in the 256-rank comparisons, while QuEST’s interval excludes sampling. The sampled-output checks compare distributions at their stated shot budget rather than state fidelity.

## Software

The QSIMADV NVIDIA build uses NVHPC SDK 26.3 (CUDA 13.1, cuBLAS, cuTensor 2.6, NVPL, and HPC-X), gcc 13.3.0, Eigen 3.4.0, and Boost; host BLAS is NVPL. The Hopper AER baselines use CUDA 12.8 and Python 3.10.14. The AMD build uses ROCm 7.2.3, Cray MPICH, the site Eigen/OpenBLAS/Boost modules, and gcc 14.2.0. The paired Ella single-GPU and Setonix single-GCD QSIMADV builds use revisions 07fef3aa and acaeca61, respectively; data/README.md records the full hashes. Baseline versions are AER 0.17.2, QuEST 4.3, Qulacs 0.6.13, LIGHTNING-GPU 0.44.0, and LIGHTNING-KOKKOS 0.45.0.

## Numerical configuration

The Ella and Setonix random-brickwork inputs use generator seed 0, and the per-shot random-number seed is fixed within each series. These choices reproduce the tested inputs and samples; series based on one timed observation do not estimate timing variance.

## Data and provenance

GPU state-vector rows use complex-FP32 storage for QSIMADV, AER with precision=’single’, and FP32 QuEST builds unless a caption states otherwise. LIGHTNING- GPU, LIGHTNING-KOKKOS, and Qulacs-GPU use complex- FP64 storage in the cited tables. CPU state-vector rows use complex FP64 except the explicitly marked qsim-CPU series. Tableau updates are bitwise and deterministic for a fixed build and input. Workload circuits are committed as OpenQASM 3.0 files. A fixed-seed generator reproduces the random-brickwork inputs rand{N}x{d}; GHZ circuits and small fixtures are committed verbatim. Raw application rows are included in the manuscript repository, and raw rows for the paired Ella single-GPU and Setonix single-GCD trials are versioned in the simulator repository. For the remaining series, the public artefact contains On GH200, --gemmPrec tf32 selects TF32 multipli- cation with FP32 accumulation, while --gemmPrec fp32 selects strict FP32 GEMM. The HIP measurements use FP32 arithmetic. State-vector sampling uses a hierarchical cumulative distribution: single-GPU execution uses 210-entry blocks with FP64 block sums and search, and local CPU execution uses an FP64 CDF with 216-entry blocks. MPI-GPU stores within- block prefixes in FP32 and block and inter-rank offsets in FP64; MPI-CPU uses a full FP64 local CDF. B. Fusion configuration For state-vector execution, the CUDA GPU path uses Kmax = 7 at N ≥30 and Kmax = 5 below that threshold; CPU uses Kmax = 5 and HIP uses Kmax = 6. The marked- diagonal cap is 6 on GPU and 5 on CPU. In the two instantaneous-quantum-polynomial cells (iqp{26,30}d5) and the rand30x50 ablation, the dynamic-programming partition absorbs diagonal operations into mixed fused blocks; replacing its score with a diagonal- aware variant changes the Hopper intervals by less than 2%.

## Execution configuration

Single-node CPU comparisons use 70 of 72 Grace cores on Ella and 64 threads on Setonix; each reported tool uses its fastest measured binding policy. The Setonix CPU-MPI sweep assigns one node and 64 OpenMP threads per rank. C. 32-bit limits in distributed exchange Two distinct 32-bit limits affect distributed execution. The full-permutation helper stores the destination offset of every local amplitude as a signed 32-bit integer. Because a rank holds 2Qloc amplitudes, this routing table is valid only for Qloc ≤30. The restriction belongs to this helper, which is used for Qloc < 4 or when unchunked execution is requested; it is not a bound on the distributed engine. Run records include the resolved simulation method, fusion- width cap, 64-operation DP window, cross-window merge, arithmetic mode, and distributed lowering. GPU-MPI searches 1024 entries of the gate/noise target schedule with bounded- lookahead farthest-next-use; CPU-MPI uses first-free placement. Terminal count sampling decodes the final layout map without restoring canonical state-vector order. The default dispatcher instead partitions the local state into chunks and realises a permutation through intra-rank swaps and XOR-partner or coalesced cross-rank transfers. It does not construct the full 32-bit destination-index table, and transfers larger than an MPI call can represent are split into pieces. The default chunk width is B = Qloc −1 for Qloc ≥4, so a generic fused block must satisfy k ≤B. The large-Qloc weak-scaling runs use this path.

## APPENDIX C
SUPPORTING ANALYSIS AND MEASUREMENTS

This appendix collects the complexity summary, implemen- tation bounds, correctness and profile evidence, and per-cell measurements underlying §V. Times are in seconds. Bold marks the minimum within a row when tools are columns and within a rank column when tools are rows. The second limit is the signed 32-bit count accepted by the evaluated MPI interfaces. QSIMADV counts typed complex elements rather than bytes and keeps every call below INT_MAX; for example, 228 FP64 complex amplitudes have a 4 GiB payload but a representable element count. AER counts bytes for its swap messages, so the evaluated FP32 GPU baselines use blocking_qubits=27 and the FP64 CPU-MPI baseline uses 26 to keep each call below 2 GiB. On the rand30x50 GPU strong-scaling axis, the FP32 limit is reached at P = 2 and P = 4 on both platforms, so those AER cells use the blocked exchange. A. Engine bounds and concrete dispatch The factory selects a concrete engine. State-vector and density-matrix paths instantiate their supported widths as template arguments, making dimensions, index masks, and layouts compile-time constants; MPS, tensor-network, and tableau storage is sized at runtime. Distributed state-vector paths instantiate the local width Qloc rather than the global qubit count N. D. Proof of the distributed layout invariant Tab. VII lists the factory’s dispatch ranges. Requests outside these ranges are rejected; instances within them remain subject to memory and workspace limits. The density-matrix path reuses the GPU state-vector machinery at width 2N under the vectorisation convention of §IV-B. We expand Theorem 1. Let πt map each logical qubit to its current physical index-bit position, and let Ππt be the corresponding basis permutation from §IV-D. If |ψt⟩is the canonical logical state and the stored state, the invariant is  eψt Each evaluated binary selects a CPU, CUDA, or HIP backend at build time; MPI builds distribute the corresponding local state-vector path. Tab. I summarises their native lowerings, Appendix A gives the build recipes and software stacks, and Appendix B defines the numerical modes. = Ππt |ψt⟩. Equivalently, for every logical bit string x, the amplitude at the physical bit string y satisfying yπt(q) = xq equals the logical amplitude at x. On the evaluated CPU path, diagonal blocks stream in place. Dense blocks use direct gather–multiply–scatter kernels for k ≤3, and for k = 4, 5 when the local state contains at least 223 amplitudes; all other dense blocks use host permutation and complex GEMM. CPU-MPI applies the same rule within each rank. Lemma 1 (Local mapped operator). For a fused operator G on ordered logical targets V = (q0, . . . , qk−1), set A = πt(V ). If all positions in A are local, an order-preserving local lowering preserves the invariant with πt+1 = πt. If the lowering retains a permutation αA of the local bit positions, the invariant is Table VI. Asymptotic costs of the algorithms used in QSIMADV. Here N is the qubit count, k the fused-block width, |C| the parsed instruction count, W the fusion-window limit, Kmax the fusion cap, Kblk the larger of Kmax and the width of the widest unitary noise-free input gate, nmeas the measurement count, P = 2r the MPI rank count, and Qloc = N −r the local index-bit count. “Time cost” separates arithmetic from communication or memory traffic; “working set” is the dominant in-memory data structure.

## Algorithm
Time cost
Working set
Reference

2N + 4k Eqs. (4)–(5) Diagonal fused block Θ(2N) O Generic permutation–GEMM (per block) arithmetic Θ(2N+k) complex multiply-adds; permutation and state-update traffic each Θ(2N) for fixed k O 2N Eq. (6) Materialised density superoperator (application) arithmetic O 4N + 16k §IV-B DP fusion (whole circuit) partition DP O(|C| · W · Kblk); dense block-matrix construction O 16k O 4N ; operator read O 22N+2k ; state traffic O |C| · 4Kblk |C| · 8Kmax ; DP auxiliary O(W + Kblk) Eq. (7) Stabiliser Clifford gate (per op) Θ(⌈2N/64⌉) updates to 64-bit integers O circuit matrices O bits Eq. (1) Stabiliser (general per shot) O |C| · N + (nmeas + 1) · N 2 updates to 64-bit integers O bits Eq. (2) Lazy MPI placement (per block) one promotion moves Θ(2N/P) amplitudes per rank; local update O 2N/P + 4k + N  2k+Qloc O Eq. (10) These lemmas prove the theorem by induction over promo- tion swaps and fused operators. A terminal computational-basis sampler need not invert the permutation: it samples a physical basis index and decodes each logical output bit through πt. A consumer that requires canonical amplitude order may instead request materialisation of the inverse permutation. Table VII. Factory dispatch ranges and state representations. The ranges are acceptance bounds, not memory guarantees.

## Engine or path
Accepted range
State representation

dense 3 ≤N ≤39 CPU state vector with complex FP64 amplitudes gpu 3 ≤N ≤35 CUDA or HIP state vector with complex FP32 amplitudes mps N ≤200 Runtime-sized MPS with a bond cap and singular-value cutoff cutn N ∼50 Runtime-sized NVIDIA tensor network; absent from the evaluated builds density_ matrix E. Precision comparison The first two columns of Tab. VIII hold the AER-noCuQ backend fixed and change its configured precision from FP64 to FP32. The FP32 configuration reduces its interval by 1.46– 1.86× across the sweep. It is therefore the relevant matched- storage AER-noCuQ baseline for QSIMADV: AER is faster at N = 24 and 26, and QSIMADV is faster from N = 28. 2 ≤N ≤17 Vectorised density matrix tableau (routed) not template- bounded -bit stabiliser tableau MPI GPU 3 ≤Qloc ≤35 Distributed state vector with complex FP32 amplitudes MPI CPU 3 ≤Qloc ≤39 Distributed state vector with complex FP64 amplitudes Table VIII. Precision comparison on Ella; entries are five-run medians in seconds. AER denotes AER-noCuQ, and the ratio uses its FP32 column. QSIMADV stores FP32 states and uses TF32 multiplication with FP32 accumulation. preserved with πt+1 = ˆαA ◦πt, where ˆαA leaves the rank- address positions fixed. circuit AER FP64 AER FP32 QSIMADV FP32 ratio rand24x50 0.127 0.087 0.256 0.34× rand26x50 0.413 0.238 0.410 0.58× rand28x50 1.701 0.949 0.901 1.05× rand30x50 7.124 3.821 1.932 1.98× rand32x50 30.13 16.27 7.415 2.19× Proof. By definition of Ππt, selecting the target bits at physical positions A = πt(V ) selects the same ordered subspace index as selecting V in the logical state. Let GW denote G acting on the ordered targets W. The order-preserving case follows from Gπt(V )Ππt = ΠπtGV . F. Capacity and execution profiles If the lowering retains αA, the additional basis permutation gives a) GH200: A complex FP32 state vector occupies 8 · 2N bytes, and the permutation–GEMM path holds both input and output vectors. Its two-vector baseline is therefore 16 GiB at N = 30 and 64 GiB at N = 32. The measured footprints are 22.5 and 70.5 GiB, respectively; the additional 6.5 GiB is library workspace. The N = 32 case remains within the GH200’s 96 GiB device memory. An external device-kernel trace of rand32x20 attributes 1.95 s to cuTensor permutation and 1.81 s to cuBLAS GEMM, with negligible sampling time, showing that the two full-state terms have comparable cost in this run. ΠˆαAΠπtGV |ψt⟩= ΠˆαA◦πtGV |ψt⟩, which proves the second case. Lemma 2 (Distributed promotion swap). Suppose a target lies at rank-address position b ≥Qloc and ℓ< Qloc is an unpinned local position. Let τ exchange physical positions b and ℓ. A distributed bit transpose implementing Πτ, followed by the map update πt+1 = τ ◦πt, preserves the invariant and makes that target local. b) MI250X: The same N = 32 two-vector baseline fills the 64 GiB available to one GCD before workspace is added, so rand32x50 runs through managed-memory paging. The matched-FP32 QuEST-GPU build uses one in- place state vector and completes in 148 s, compared with 626.6 s for QSIMADV. The available host profile does not support a kernel-level split: fastTranspose accounts for 91% of the interval because it synchronises the stream and therefore absorbs preceding asynchronous work and paging, while fastMatmulGPU records only 0.010 s. At N ≥32, Proof. The transpose changes the stored state to = ΠτΠπt |ψt⟩= Πτ◦πt |ψt⟩. The logical state is unchanged during promotion, and the updated map records exactly the physical transposition. Because b is exchanged with a local position, the promoted target becomes local. The condition k ≤Qloc guarantees that enough unpinned local positions exist for all targets of the fused operator. QSIMADV uses a custom dense-update kernel with 64-bit offsets because the tested rocBLAS cgemm interface uses 32-bit indices. Table XI. CPU-MPI state-vector strong scaling on rand30x50 over Ella. “T/O” denotes timeout.

## P
QSIMADV-CPU
AER-CPU
QuEST-CPU

1 87.79 T/O 330.70 2 191.68 2001.9 208.65 4 162.00 975.3 133.32 8 151.28 503.9 88.15 G. Per-platform measurement tables The tables below provide the CPU, MPI, and large-scale weak-scaling values behind the evaluation figures. Fig. 8 combines the QFT rows from the four single-device tables. Table XII. Single-process CPU intervals on Setonix. qsim-CPU uses FP32; the other engines use FP64. The panels share rows; “T/O” denotes timeout and “N/M” an unmeasured cell. 1) Ella: The following tables give the Ella CPU and MPI data behind Fig. 4; the single-GPU cells are in Tab. III.

## Circuit
QSIMADV
AER
qsim-CPU

rand24x50 2.150 3.071 1.164 rand26x50 18.21 8.986 2.361 rand28x50 56.98 40.37 19.68 rand30x50 243.6 176.4 55.65 qft24 0.364 0.224 0.282 qft26 4.494 1.179 1.260 qft28 24.17 4.498 6.658 qft30 111.6 19.31 27.16 Table IX. Single-process CPU intervals on Ella. qsim-CPU uses FP32; the other engines use FP64. The panels share rows; “T/O” denotes timeout.

## Circuit
QSIMADV
AER
qsim-CPU

rand24x50 1.139 1.044 0.531 rand26x50 7.062 4.799 2.531 rand28x50 20.85 22.64 11.85 rand30x50 88.31 98.33 50.64 qft24 0.228 0.132 0.121 qft26 1.070 0.674 0.978 qft28 5.113 2.996 4.938 qft30 22.90 13.47 21.96

## Circuit
LIGHTNING
Qulacs
QuEST-CPU

rand24x50 71.27 3.878 3.275 rand26x50 T/O 16.73 27.28 rand28x50 T/O 92.36 102.1 rand30x50 T/O T/O 405.6 qft24 3.620 0.555 0.479 qft26 16.94 3.539 2.385 qft28 76.24 16.22 9.727 qft30 N/M N/M 42.34

## Circuit
LIGHTNING
Qulacs
QuEST-CPU

rand24x50 80.24 3.133 3.286 rand26x50 240.8 12.43 11.97 rand28x50 T/O 80.43 78.42 rand30x50 T/O 337.2 332.1 qft24 2.217 0.476 0.251 qft26 10.59 2.795 1.482 qft28 47.55 13.72 7.177 qft30 208.4 62.91 33.20 closest-GPU binding; the two- and four-rank AER cells use blocking_qubits=27 to stay below the count-width limit of Appendix C-C. The MPI tables use HPC-X OpenMPI over RoCEv2. The GPU sweep assigns one GH200 to each rank and uses complex-FP32 storage; because rand30x50 fits on one GH200, additional ranks expose distributed-execution cost rather than capacity. The AER rows at P ≥2 use blocking_qubits=27 to keep swap messages below the count-width limit of Appendix C-C. The CPU sweep assigns one Grace node to each rank and uses FP64. Table XIII. GPU-MPI strong scaling on rand30x50 over Setonix. ‡ marks AER cells with reduced blocking_qubits to keep messages representable (Appendix C-C). Tool P = 1 [s] P = 2 [s] P = 4 [s] P = 8 [s] QSIMADV 8.63 7.08 4.14 2.55 AER-GPU 18.35 12.68‡ 7.48‡ 4.65 QuEST-GPU 34.47 32.16 25.80 17.44 Table X. GPU-MPI strong scaling on rand30x50 over Ella. QSIMADV cells are five-run medians; each AER cell is one warm observation. “T/O” denotes timeout. The CPU-MPI sweep assigns one EPYC node and 64 OpenMP threads to each rank; all three tools use the same Cray MPICH stack. The AER CPU-MPI rows use chunked blocking with blocking_qubits=26, whereas the GPU-MPI rows above use 27. P QSIMADV AER-noCuQ AER-cuQ† 1 1.97 3.94 3.67 2 4.28 4.97 T/O 4 5.37 3.83 4.25 8 4.32 4.11 4.66 Table XIV. CPU-MPI state-vector strong scaling on rand30x50 over Setonix. † closed-source CUQUANTUM.

## P
QSIMADV-CPU
AER-CPU
QuEST-CPU

1 245.46 3044.9 451.43 2 232.04 1807.1 271.45 4 143.57 872.2 167.97 8 86.83 419.7 104.64 The small-rank GPU weak-scaling series in Fig. 6 holds Qloc = 31 and reaches N = 34. The AER CPU-MPI build uses its chunked blocking_enable configuration, so its P = 1 row is distinct from the single-process configuration in Tab. IX. For large-scale weak scaling, N increases with P while the rank-local state size remains fixed. The GPU sweep uses Qloc = 30 (one MI250X GCD per rank, complex-FP32 storage), and the CPU sweep uses Qloc = 28 (one EPYC node per rank, FP64). 2) Setonix: The following tables give the Setonix CPU, strong-scaling, and capacity results behind Fig. 5 and Fig. 7; the single-GCD cells are in Tab. IV. The MPI comparisons use Cray MPICH over Slingshot/CXI. GPU runs use one rank per node and one GCD per rank with (a) NVIDIA Hopper · GPU (b) AMD MI250X · one GCD qsim (Google) Lightning Qulacs QuEST-GPU

## QuEST-GPU
qsimadv

reported interval (s) reported interval (s) N=32: Aer-noCuQ/qsimadv = 1.44× N=30: Aer-GPU/qsimadv = 1.03× 24 26 28 30 32 qubits N 24 25 26 27 28 29 30 qubits N (c) NVIDIA Grace · CPU (d) AMD EPYC 7763 · CPU Aer qsim (Google) Lightning

## Qulacs
QuEST-CPU
qsimadv

reported interval (s) reported interval (s) N=30: qsimadv/Aer = 1.70× N=30: qsimadv/Aer = 5.78× 24 25 26 27 28 29 30 qubits N 24 25 26 27 28 29 30 qubits N Fig. 8. Single-device QFT intervals on (a) Ella Hopper, (b) one Setonix MI250X GCD, (c) Ella Grace, and (d) one Setonix EPYC node. LIGHTNING-GPU, Qulacs-GPU, and LIGHTNING-KOKKOS use FP64; qsim-CPU uses FP32. Values: Tabs. III, IV, IX, and XII. H. Semantic routing and application workloads Table XV. Setonix GPU-MPI weak scaling on depth-20 random brickwork at Qloc = 30. The routing benchmark contains eight GHZ circuits with per-gate depolarising noise at pdep = 10−2 and two noiseless random-Clifford circuits. Each QSIMADV input is run once with ordinary semantic routing and once with routing disabled to force state-vector execution where it fits. Every ordinary request selects the tableau; the forced state-vector series ends at N = 30. AER’s stabiliser sampler and Stim provide dedicated stabiliser baselines in Tab. XVII. P N nodes QSIMADV [s] AER-GPU [s] 8 33 1 18.93 37.54 16 34 2 21.09 33.70 32 35 4 20.85 22.75 64 36 8 26.15 20.41 128 37 16 22.82 17.46 256 38 32 31.66 28.07 For each GHZ cell, let ˆpi be tool i’s observed fre- quency of the all-zero outcome 0N at S = 1000. For tools i, j, the independent binomial standard error is seij = p Table XVI. Setonix CPU-MPI weak scaling on depth-20 random brickwork at Qloc = 28. “N/M” denotes an unmeasured cell. P N total state QSIMADV [s] AER-CPU [s] QuEST [s] ˆpi(1 −ˆpi)/S + ˆpj(1 −ˆpj)/S. The largest defined ratio |ˆpi− ˆpj|/seij is 1.02. This check covers one sampled GHZ marginal; it does not cover the random-Clifford outputs. 2 29 8 GiB 39.30 332.45 38.63 4 30 16 GiB 42.32 303.83 49.21 8 31 32 GiB 45.30 335.26 60.11 16 32 64 GiB 50.58 329.91 71.23 32 33 128 GiB 64.77 363.28 82.68 64 34 256 GiB 81.64 415.89 95.06 128 35 512 GiB 84.97 407.68 107.05 256 36 1 TiB 112.44 381.91 N/M The application set comprises QAOA size and depth sweeps, one-layer UCCSD-style H2, LiH, and BeH2 circuits, and four QASMBench inputs, including the Clifford qec9xz_n17 case. Each shared OpenQASM 3.0 input supported by all three tools

## REFERENCES

Table XVII. Synthetic Clifford routing on Ella. QSIMADV tab and QSIMADV SV denote routed-tableau and forced-state-vector modes; “N/A” denotes an unavailable state-vector cell. Circuit N QSIMADV tab AER stab Stim QSIMADV SV [1] H. Bayraktar, A. Charara, D. Clark, S. Cohen, T. Costa, Y.-L. L. Fang, Y. Gao, J. Guan, J. Gunnels, A. Haidar, A. Hehn, M. Hohnerbach, M. Jones, T. Lubowe, D. Lyakh, S. Morino, P. Springer, S. Stanwyck, I. Terentyev, S. Varadhan, J. Wong, and T. Yamaguchi, “cuQuantum SDK: a high-performance library for accelerating quantum science,” in 2023 IEEE International Conference on Quantum Computing and Engineering (QCE), 2023, pp. 1050–1061. [2] G. Aleksandrowicz et al., “Qiskit: An open-source framework for ghz20 20 0.0061 0.0020 0.00005 1.284 ghz25 25 0.0056 0.0028 0.00006 12.45 ghz30 30 0.0058 0.0026 0.00007 454.8 ghz60 60 0.0073 0.0045 0.00011 N/A ghz100 100 0.0097 0.0082 0.00020 N/A ghz200 200 0.0198 0.0243 0.00037 N/A ghz500 500 0.129 0.174 0.00090 N/A ghz1000 1000 0.884 1.195 0.00164 N/A randcliff100 100 0.113 0.204 0.00041 N/A randcliff500 500 4.715 12.81 0.00585 N/A quantum computing,” Zenodo software record, 2019. [Online]. Available: https://doi.org/10.5281/zenodo.2562111 [3] Qiskit Development Team, “AerSimulator: simulation method options,” 2025, qiskit Aer documentation, accessed: Jul. 31, 2026. [Online]. Available: https://qiskit.github.io/qiskit-aer/stubs/qiskit aer.AerSimulator. html [4] A. Asadi, A. Dusko, C.-Y. Park, V. Michaud-Rioux, I. Schoch, S. Shu, is run with QSIMADV, AER, and LIGHTNING on one Hopper GPU. Tab. XVIII reports five-trial medians under the runner boundaries of Appendix B. T. Vincent, and L. J. O’Riordan, “Hybrid quantum programming with PennyLane Lightning on HPC platforms,” arXiv preprint arXiv:2403.02512, 2024. [Online]. Available: https://arxiv.org/abs/2403. 02512 [5] S. V. Isakov, D. Kafri, O. Martin, C. Vollgraff Heidweiller, W. Mruczkiewicz, M. P. Harrigan, N. C. Rubin, R. Thomson, M. Broughton, K. Kissell, E. Peters, E. Gustafson, A. C. Y. Li, H. Lamm, G. Perdue, A. K. Ho, D. Strain, and S. Boixo, “Simulations of quantum circuits with approximate noise using qsim Table XVIII. Five-trial application-workload medians on Ella under the tool- native timing policy of Appendix B. N is the number of acted-on qubits.

## Circuit
N
QSIMADV
AER LIGHTNING

QAOA, 2 layers: size sweep qaoa18p2_ring 18 0.572 0.375 0.127 qaoa22p2_ring 22 0.574 0.385 0.143 qaoa26p2_ring 26 0.625 0.426 0.233 qaoa18p2_3reg 18 0.583 0.333 0.130 qaoa22p2_3reg 22 0.585 0.395 0.147 qaoa26p2_3reg 26 0.616 0.424 0.265 QAOA at N = 30: one to three layers qaoa30p1_ring 30 0.933 0.698 1.094 qaoa30p2_ring 30 1.013 0.988 1.972 qaoa30p3_ring 30 1.085 1.205 2.849 qaoa30p1_3reg 30 0.978 0.758 1.365 qaoa30p2_3reg 30 1.070 1.107 2.511 qaoa30p3_3reg 30 1.233 1.424 3.667 UCCSD-style, 1 layer uccsd_h2 8 0.603 0.401 0.190 uccsd_lih 12 0.756 0.658 1.137 uccsd_beh2 14 1.279 1.048 2.875 QASMBench medium multiply_n13 13 0.586 0.322 0.127 multiplier_n15 15 0.580 0.327 0.130 dnn_n16 16 0.591 0.412 0.306 qec9xz_n17 17 0.071 0.430 0.125 and Cirq,” arXiv preprint arXiv:2111.02396, 2021. [Online]. Available: https://arxiv.org/abs/2111.02396 [6] W. M. Brown, A. Ramesh, T. Lubinski, T. Nguyen, and D. E. Bernal Neira, “Multi-gpu quantum circuit simulation and the impact of network performance,” Computer Physics Communications, vol. 324, p. 110126, 2026. [7] Pawsey Supercomputing Research Centre, “Setonix,” n.d., accessed: Jul. 14, 2026. [Online]. Available: https://pawsey.org.au/systems/setonix/ [8] Advanced Micro Devices, “ROCm 7.2.3 release notes,” 2026. [Online]. Available: https://rocm.docs.amd.com/en/docs-7.2.3/about/release-notes. html [9] G. Vidal, “Efficient classical simulation of slightly entangled quantum computations,” Phys. Rev. Lett., vol. 91, no. 14, p. 147902, 2003. [10] D. Gottesman, “The heisenberg representation of quantum computers,” in Group22: Proceedings of the XXII International Colloquium on Group Theoretical Methods in Physics. International Press, 1999, pp. 32–43. [11] S. Aaronson and D. Gottesman, “Improved simulation of stabilizer circuits,” Phys. Rev. A, vol. 70, no. 5, p. 052328, 2004. [12] Y. Suzuki, Y. Kawase, Y. Masumura, Y. Hiraga, M. Nakadai, J. Chen, K. M. Nakanishi, K. Mitarai, R. Imai, S. Tamiya, T. Yamamoto, T. Yan, T. Kawakubo, Y. O. Nakagawa, Y. Ibe, Y. Zhang, H. Yamashita, H. Yoshimura, A. Hayashi, and K. Fujii, “Qulacs: a fast and versatile quantum circuit simulator for research purpose,” Quantum, vol. 5, p. 559, 2021. [13] T. Jones, A. Brown, I. Bush, and S. C. Benjamin, “QuEST and high performance simulation of quantum computers,” Scientific Reports, vol. 9, no. 1, p. 10736, 2019. [14] H. De Raedt, F. Jin, D. Willsch, M. Willsch, N. Yoshioka, N. Ito, S. Yuan,

## ACKNOWLEDGMENT

and K. Michielsen, “Massively parallel quantum computer simulator, eleven years later,” Computer Physics Communications, vol. 237, pp. 47–61, 2019. [15] A. Li, B. Fang, C. Granade, G. Prawiroatmodjo, B. Heim, M. Roetteler, This work was supported by resources provided by the Pawsey Supercomputing Research Centre with funding from the Australian Government and the Government of Western Australia. It was carried out within the Pawsey Supercomputing Research Centre’s Quantum Supercomputing Innovation Hub, made possible by a grant from the Australian Government through the National Collaborative Research Infrastructure Strategy (NCRIS). Computational resources were provided by the Pawsey Supercomputing Research Centre’s Setonix Supercomputer (https://doi.org/10.48569/18sb-8s43), with fund- ing from the Australian Government and the Government of Western Australia. and S. Krishnamoorthy, “SV-Sim: Scalable PGAS-based state vector simulation of quantum circuits,” in Proceedings of the International Conference for High Performance Computing, Networking, Storage and Analysis, 2021, pp. 1–14. [16] M. Xu, S. Cao, X. Miao, U. A. Acar, and Z. Jia, “Atlas: Hierarchical partitioning for quantum circuit simulation on GPUs,” in SC24: Interna- tional Conference for High Performance Computing, Networking, Storage and Analysis, 2024, pp. 1–17. [17] C. Gidney, “Stim: a fast stabilizer circuit simulator,” Quantum, vol. 5, p. 497, 2021. [18] J. Gray, “quimb: a Python package for quantum information and many- body calculations,” Journal of Open Source Software, vol. 3, no. 29, p. 819, 2018. [19] S.-X. Zhang, J. Allcock, Z.-Q. Wan, S. Liu, J. Sun, H. Yu, X.-H. Yang, AI-assisted tools were used during the development of portions of the software implementation. All generated code and related documents were reviewed, validated, and modified by the authors. J. Qiu, Z. Ye, Y.-Q. Chen, C.-K. Lee, Y.-C. Zheng, S.-K. Jian, H. Yao, C.- Y. Hsieh, and S. Zhang, “TensorCircuit: a quantum software framework for the NISQ era,” Quantum, vol. 7, p. 912, 2023. [20] Google Quantum AI, “qsim v0.22.0 documentation: AMD GPU and [36] A. Li, O. Subasi, X. Yang, and S. Krishnamoorthy, “Density matrix MPS support,” Software documentation, 2026, accessed 2026-07-15. [Online]. Available: https://github.com/quantumlib/qsim/tree/v0.22.0/docs [21] A. W. Cross, A. Javadi-Abhari, T. Alexander, N. de Beaudrap, L. S. quantum circuit simulation via the BSP machine on modern GPU clusters,” in SC20: International Conference for High Performance Computing, Networking, Storage and Analysis, 2020, pp. 1–15. [37] Pacific Northwest National Laboratory, “NWQ-Sim: Northwest quantum Bishop, S. Heidel, C. A. Ryan, P. Sivarajah, J. Smolin, J. M. Gambetta, and B. R. Johnson, “OpenQASM 3: A broader and deeper quantum assembly language,” ACM Transactions on Quantum Computing, vol. 3, no. 3, pp. 1–50, 2022. [22] P. Springer and P. Bientinesi, “Design of a high-performance GEMM- circuit simulation environment,” Software repository, 2026, accessed 2026-07-15. [Online]. Available: https://github.com/pnnl/NWQ-Sim [38] A. Tabuchi, S. Imamura, M. Yamazaki, T. Honda, A. Kasagi, H. Nakao, N. Fukumoto, and K. Nakashima, “mpiQulacs: A scalable distributed quantum computer simulator for ARM-based clusters,” in 2023 IEEE International Conference on Quantum Computing and Engineering (QCE), 2023, pp. 959–969. [39] D. S. Steiger, T. H¨aner, and M. Troyer, “ProjectQ: an open source like tensor–tensor multiplication,” ACM Transactions on Mathematical Software, vol. 44, no. 3, pp. 1–29, 2018. [23] L. A. Bel´ady, “A study of replacement algorithms for a virtual-storage computer,” IBM Systems Journal, vol. 5, no. 2, pp. 78–101, 1966. [24] R. L. Mattson, J. Gecsei, D. R. Slutz, and I. L. Traiger, “Evaluation software framework for quantum computing,” Quantum, vol. 2, p. 49, 2018. [40] MindSpore, “MindSpore Quantum Documentation,” n.d., accessed: Jul. techniques for storage hierarchies,” IBM Systems Journal, vol. 9, no. 2, pp. 78–117, 1970. [25] M. Smelyanskiy, N. P. D. Sawaya, and A. Aspuru-Guzik, “qHiPSTER: 14, 2026. [Online]. Available: https://www.mindspore.cn/mindquantum/ docs/en/stable/index.html [41] X.-Z. Luo, J.-G. Liu, P. Zhang, and L. Wang, “Yao.jl: Extensible, efficient the quantum high performance software testing environment,” arXiv preprint arXiv:1601.07195, 2016. [Online]. Available: https://arxiv.org/ abs/1601.07195 [26] T. H¨aner and D. S. Steiger, “0.5 petabyte simulation of a 45-qubit framework for quantum algorithm design,” Quantum, vol. 4, p. 341, 2020. [42] H. C. Edwards, C. R. Trott, and D. Sunderland, “Kokkos: enabling quantum circuit,” in Proceedings of the International Conference for High Performance Computing, Networking, Storage and Analysis (SC ’17). ACM, 2017, pp. 1–10. [27] C. Zhang, Z. Song, H. Wang, K. Rong, and J. Zhai, “HyQuas: hybrid manycore performance portability through polymorphic memory access patterns,” Journal of Parallel and Distributed Computing, vol. 74, no. 12, pp. 3202–3216, 2014. [43] OpenMP Architecture Review Board, “OpenMP application programming partitioner based quantum circuit simulation system on GPU,” in Proceedings of the ACM International Conference on Supercomputing (ICS ’21), 2021, pp. 443–454. [28] E. Farhi, J. Goldstone, and S. Gutmann, “A quantum approximate interface, version 5.1,” 2020. [Online]. Available: https://www.openmp. org/spec-html/5.1/openmp.html [44] Khronos SYCL Working Group, “SYCL 2020 specification, revision 11,” 2025. [Online]. Available: https://registry.khronos.org/SYCL/specs/ sycl-2020/html/sycl-2020.html [45] F. G. Van Zee and R. A. van de Geijn, “BLIS: a framework for rapidly optimization algorithm,” arXiv preprint arXiv:1411.4028, 2014. [Online]. Available: https://arxiv.org/abs/1411.4028 [29] A. Peruzzo, J. McClean, P. Shadbolt, M.-H. Yung, X.-Q. Zhou, P. J. instantiating BLAS functionality,” ACM Transactions on Mathematical Software, vol. 41, no. 3, pp. 14:1–14:33, 2015. Love, A. Aspuru-Guzik, and J. L. O’Brien, “A variational eigenvalue solver on a photonic quantum processor,” Nature Communications, vol. 5, no. 1, p. 4213, 2014. [30] A. Li, S. Stein, S. Krishnamoorthy, and J. Ang, “QASMBench: A low- level quantum benchmark suite for NISQ evaluation and simulation,” ACM Transactions on Quantum Computing, vol. 4, no. 2, pp. 1–26, 2023. [31] B. Villalonga, D. Lyakh, S. Boixo, H. Neven, T. S. Humble, R. Biswas, E. G. Rieffel, A. Ho, and S. Mandr`a, “Establishing the quantum supremacy frontier with a 281 Pflop/s simulation,” Quantum Science and Technology, vol. 5, no. 3, p. 034003, 2020. [32] K. De Raedt, K. Michielsen, H. De Raedt, B. Trieu, G. Arnold, M. Richter, T. Lippert, H. Watanabe, and N. Ito, “Massively parallel quantum computer simulator,” Computer Physics Communications, vol. 176, no. 2, pp. 121–136, 2007. [33] C. Zhang, H. Wang, Z. Ma, L. Xie, Z. Song, and J. Zhai, “UniQ: a unified programming model for efficient quantum circuit simulation,” in Proceedings of the International Conference for High Performance Computing, Networking, Storage and Analysis (SC ’22), 2022, pp. 1–16. [34] Y. Teranishi, S. Hiraoka, W. Mizukami, M. Okita, and F. Ino, “Lazy qubit reordering for accelerating parallel state-vector-based quantum circuit simulation,” ACM Transactions on Quantum Computing, vol. 6, no. 4, pp. 1–33, 2025. [Online]. Available: https://arxiv.org/abs/2410.04252 [35] G. Zhong, Y. Fan, and Z. Li, “Scalable parallel simulation of quantum circuits on CPU and GPU systems,” arXiv preprint arXiv:2509.04955, 2025. [Online]. Available: https://arxiv.org/abs/2509.04955

