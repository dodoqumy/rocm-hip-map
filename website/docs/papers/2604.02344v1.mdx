---
title: "Characterizing WebGPU Dispatch Overhead for LLM Inference Across Four GPU Vendors, Three Backends, and Three Browsers"
description: "Characterizing WebGPU Dispatch Overhead for LLM Inference Ac"
keywords: ["AMD", "GPU", "GPU编程", "HPC", "ROCm", "arXiv", "bilingual", "中英对照", "人工智能", "双语", "学术", "并行计算", "深度学习", "翻译", "论文"]
source_url: https://arxiv.org/abs/2604.02344v1
source_type: paper
source_org: arxiv
credibility: 5
lifecycle: latest
version: ""
rocm_versions: []
os: []
gpu: []
gpu_arch: []
driver: []
frameworks: []
difficulty: ""
tags: []
published_date: ""
synced_date: "2026-04-29"
generated_at: "2026-04-29"
arxiv_id: "2604.02344v1"
---

import ArticleHeader from "@site/src/components/ArticleHeader";
import PaperArticleHeader from "@site/src/components/PaperArticleHeader";


<ArticleHeader />
<PaperArticleHeader />

> 📄 **arXiv：** [https://arxiv.org/abs/2604.02344v1](https://arxiv.org/abs/2604.02344v1)  
> 🏷 **来源：** 学术论文 · 同行评审：⭐⭐⭐⭐⭐

---

## 摘要

WebGPU's security-focused design imposes per-operation validation that compounds across the many small dispatches in neural network inference, yet the true cost of this overhead is poorly characterized. We present a systematic characterization of WebGPU dispatch overhead for LLM inference at batch size 1, spanning four GPU vendors (NVIDIA, AMD, Apple, Intel), two native implementations (Dawn, wgpu-native) and three browsers (Chrome, Safari, Firefox), and two model sizes (Qwen2.5-0.5B and 1.5B). Our primary contribution is a sequential-dispatch methodology that reveals naive single-operation benchmarks overestimate dispatch cost by $&#123;\sim&#125;20\times$. The true per-dispatch cost of WebGPU API overhead alone is 24-36 $μ$s on Vulkan and 32-71 $μ$s on Metal, while the total per-operation overhead including Python cost is $&#123;\sim&#125;95$~$μ$s, which turns out to be a distinction critical for optimization. On Vulkan, kernel fusion improves throughput by 53%, while CUDA fusion provides no benefit, confirming that per-operation overhead is a primary differentiator. LLM inference was tested across three major operating systems (Linux, Windows, macOS). We built $\texttt&#123;torch-webgpu&#125;$, a PrivateUse1-based out-of-tree PyTorch backend and an FX-to-WebGPU compiler, which on our reference platform achieves 11--12% of CUDA performance. At dtype-matched float32, RTX PRO 2000 achieves 1.4$\times$ WebGPU's throughput despite $&#123;\sim&#125;6\times$ less compute than RTX 5090. For dispatch overhead, backend choice is the dominant factor, although implementation choice also matters substantially within a backend (2.2$\times$ for Metal). In terms of dispatch vs kernel compute efficiency, we conclude that at batch=1 with the current dispatch-heavy pipeline, per-operation overhead dominates regardless of kernel quality. All code, benchmarks, and raw data are open source.

Characterizing WebGPU Dispatch Overhead for LLM Infer- ence Across Four GPU Vendors, Three Backends, and Three Browsers Jędrzej Maczan Independent Researcher

## Abstract

WebGPU’s security-focused design imposes per-operation validation that compounds across the many small dispatches in neural network inference, yet the true cost of this overhead is poorly characterized. We present a systematic characterization of WebGPU dispatch over- head for LLM inference at batch size 1, spanning four GPU vendors (NVIDIA, AMD, Apple, Intel), two native implementations (Dawn, wgpu-native) and three browsers (Chrome, Sa- fari, Firefox), and two model sizes (Qwen2.5-0.5B and 1.5B). Our primary contribution is a sequential-dispatch methodology that reveals naive single-operation benchmarks overesti- mate dispatch cost by ∼20×. The true per-dispatch cost of WebGPU API overhead alone is 24–36 µs on Vulkan and 32–71 µs on Metal, while the total per-operation overhead including Python/framework cost is ∼95 µs, which turns out to be a distinction critical for optimiza- tion. On Vulkan, kernel fusion reduces dispatches from 876 to 564, improving throughput by 53%, while CUDA fusion provides no benefit, confirming that per-operation overhead is a primary differentiator. End-to-end LLM inference was tested across three major oper- ating systems (Linux, Windows, macOS) with five different backends (CUDA, MPS, CPU, WebGPU/Dawn, browser/WebLLM). We built torch-webgpu, our own PrivateUse1-based out-of-tree PyTorch backend and an FX-to-WebGPU compiler, which on our reference plat- form (NVIDIA RTX 5090/Dawn/Vulkan, float32), achieves 21.0 tok/s (0.5B) and 17.9 tok/s (1.5B), which corresponds to 11–12% of CUDA performance. At dtype-matched float32, a mobile CUDA GPU (RTX PRO 2000) achieves 1.4× WebGPU’s throughput despite ∼6× less compute than RTX 5090. For dispatch overhead, backend choice (Vulkan vs. Metal) is the dominant factor, although implementation choice also matters substantially within a backend (2.2× for Metal). In terms of dispatch vs kernel compute efficiency, we conclude that at batch=1 with the current dispatch-heavy pipeline, per-operation overhead dominates regardless of kernel quality. All code, benchmarks, and raw data are open source.

## 1
Introduction

WebGPU (W3C GPU for the Web Working Group, 2024b) provides cross-platform GPU access across all major operating systems and GPU vendors, making it a natural fit for ML deployment where portability or browser execution matters. However, WebGPU’s security-focused design imposes per-operation validation and command buffer submission, introducing overhead that compounds across the many small operations in neural network inference. The WebGPU and WGSL ecosystem is still not mature and is not widely utilized in either research or production. The performance of WebGPU implementations and specification is not well researched. Therefore, we ask what is the true per-dispatch overhead in WebGPU, how does it vary across backends, and how does it interact with framework-level and kernel-level overhead to determine end-to-end LLM inference performance? We make several contributions. First, we develop a sequential-dispatch measurement technique that reveals naive benchmarks overestimate dispatch overhead by ∼20×, and apply it across four GPU vendors, three backends, and three browsers, establishing the true per-dispatch cost at 24–36 µs (Vulkan) and 32–71 µs (Metal). We distinguish per-dispatch cost (WebGPU API overhead, directly measured) from per-operation overhead (∼95 µs, including Python/framework cost). Second, we show that kernel fusion yields 1.4× on Vulkan but provides no benefit on Metal or CUDA, providing causal evidence that per-operation overhead is the actionable bottleneck at batch size 1. Third, we provide end-to-end context: torch-webgpu (RTX 5090/Dawn, float32) achieves 21.0 tok/s (0.5B) and 17.9 tok/s (1.5B). At dtype-matched float32, a mobile GPU RTX PRO 2000 achieves only 1.4× WebGPU throughput despite ∼6× less compute than RTX 5090. Per-operation overhead is consistent across model sizes, within range of ∼95–99 µs. Finally, we characterize kernel efficiency: our unoptimized WGSL matmul reaches 1–2% of FP32 peak, though third-party evidence suggests ∼17% is achievable (Nuss, 2024) (we did not validate this ourselves). End-to-end inference was tested across all three platforms (Table 3), but the torch-webgpu backend specifically was tested only on RTX 5090/Dawn (Section 3.6).

## 2
Background

2.1 WebGPU Architecture WebGPU exposes GPU functionality through a command buffer model: operations are encoded into com- mand buffers, bound to resources via bind groups, submitted to a queue for execution, and synchronized via explicit waits or buffer mapping. This provides security (operations are validated before execution) and portability (abstraction over Vulkan, Metal, D3D12), but each dispatch adds overhead from encoder creation, bind group creation, and submission. 2.2 LLM Inference Characteristics Transformer-based LLM inference (Vaswani et al., 2017) during autoregressive generation involves many small operations per forward pass (like attention, MLP, and normalization repeated per layer), sequential token generation where each token depends on the previous output, and a required GPU→CPU synchro- nization step for token selection (argmax). All three properties add up into per-dispatch overhead. For Qwen2.5-0.5B (our primary test model), FX graph analysis reveals 1,911 total nodes per forward pass, of which approximately 876 are compute operations that potentially become WebGPU dispatches. The actual dispatch count depends on backend fusion optimizations. 2.3 Experiment Overview Table 1 classifies our experiments by scope. End-to-end LLM inference spans three platforms and five backends. Dispatch overhead benchmarks additionally cover four GPU vendors and three browsers.

## 3
Methodology

3.1 Implementations torch-webgpu: A PyTorch (Ansel et al., 2024) out-of-tree compilation backend we developed that trans- lates torch.compile() FX graphs into WGSL (W3C GPU for the Web Working Group, 2024a) shaders executed with Google’s Dawn (Google, 2024a) WebGPU implementation, which integrates with PyTorch using PrivateUse1 mechanism. Dawn dependency: Dawn is Chromium’s WebGPU implementation. Its C API tracks the evolving We- bGPU spec. Building from source requires Google’s depot_tools, creating a reproducibility challenge. The alternative, wgpu-native (Rust-based), is used in our cross-implementation validation (Section 7). ONNX Runtime (WebGPU): Microsoft’s ML runtime (Microsoft, 2024) with WebGPUExecutionProvider. We used a development build (1.24.0.dev20251218001) with ORT_ENABLE_ALL graph optimization via optimum’s ORTModelForCausalLM. This is the native version, not browser-based ONNX Runtime Web. Table 1: Classification of experiments by scope and configuration coverage

## End-to-end LLM inference

torch-webgpu E2E fp32 1 (RTX 5090/Dawn) §4 CUDA baselines E2E fp16, fp32 2 GPUs, 2 platforms §4.2 MPS baselines E2E fp16, fp32 1 (Apple M2) §4.2 CPU baselines E2E fp32 3 platforms §4.2 ONNX Runtime (WebGPU) E2E fp32 1 (RTX 5090) §4 WebLLM (browser) E2E q4f16 6 configs† App. E Dispatch overhead benchmarks (dtype-independent) Native dispatch Micro — 4 vendors, 2 impls §7.2 Browser dispatch Micro — 3 browsers, 3 platforms §7.2 RMSNorm fusion Micro fp32 5 configs §7.3 CNN/ViT/U-Net dispatch Micro — RTX 5090 ∗ Exploratory (inconclusive, appendix only) Mega-kernel Micro fp32 RTX 5090, M2 App. C Device-side argmax Micro fp32 RTX 5090, M2 App. H ∗CNN/ViT/U-Net dispatch overhead measured via exp9, exp11, exp13 (all show 24–58 µs, consistent with LLM results). †2 platforms × 3 browsers; Linux/Vulkan failed due to shader compatibility. 3.2 Hardware and Software Our primary test machine (WebGPU and CUDA benchmarks) uses an NVIDIA GeForce RTX 5090 (32GB VRAM) with an AMD Ryzen 7 9800X3D CPU, running Ubuntu 24.04 (kernel 6.14.0), PyTorch 2.9.1+cu128, ONNX Runtime 1.24.0.dev20251218001 (WebGPU provider) and Google’s Dawn. For cross-platform end-to- end inference we additionally test on a Windows 11 laptop with NVIDIA RTX PRO 2000 Blackwell (8GB) and Intel Core Ultra 7 (PyTorch 2.9.1+cu128; CUDA, CPU), and a MacBook Air with Apple M2 (8-core GPU, 16GB unified, macOS 26.2, PyTorch 2.10.0; MPS and CPU). 3.3 Benchmark Protocol We benchmark Qwen2.5-0.5B-Instruct (494M parameters, 24 layers, 896 hidden) and Qwen2.5-1.5B-Instruct (1.54B parameters, 28 layers, 1536 hidden) (Qwen Team, 2024), both with 151,936 vocabulary, on autore- gressive generation of 50 tokens from a 5-token prompt (“The capital of France is”). Each configuration is warmed up (5 runs for WebGPU/CUDA, 3 for CPU) to complete PyTorch’s Dynamo JIT compilation and stabilize performance (CV &lt; 5% post-warmup), then timed for 10–30 runs (30 for primary configurations). We report mean ± standard deviation, 95% CI (t-distribution), and coefficient of variation (CV = σ/µ). 3.4 Metrics We report three metrics. Tokens/sec is total tokens generated divided by total time. This combines prefill and decode into a single number, but for our 5-token prompts generating 50 tokens, prefill contributes less than 5% of total time, so the metric primarily reflects decode throughput. Time to first token (TTFT) measures the time from start to first token output (prefill + first decode step). Coefficient of Variation (CV) is standard deviation over mean, measuring run-to-run stability. 3.5 Measured vs Derived Values All throughput metrics (tok/s, TTFT), FX graph counts, and dispatch overhead values are directly measured. We provide two key derived values, per-operation overhead and sync overhead. Per-operation overhead = TTFTunfused −TTFTfused dispatches saved ≈95 µs Sync overhead = Ttoken −Tforward ≈11 ms 3.6 Limitations and Scope We experimented across four GPU vendors (NVIDIA, AMD, Apple, Intel) and five WebGPU implementations (Dawn, wgpu-native, Chrome 144, Safari 26.2, Firefox 147). Intel/AMD discrete GPUs remain untested for end-to-end inference. Firefox shows ∼1040 µs per-dispatch cost on all platforms. This behavior seems consistent with rate-limiting, though we did not examine Firefox source code to confirm the mechanism. Safari Metal (31.7 µs) vs. wgpu-native Metal (71 µs) confirms that the overhead is implementation-specific. Browsers end-to-end were measured via WebLLM on Windows and macOS (Table 13). We tested Qwen2.5-0.5B and 1.5B end-to-end, with per-operation overhead consistent across sizes (∼95– 99 µs), but only at batch=1 with autoregressive generation. Batch inference or different sequence lengths may behave fundamentally differently (Appendix F). A significant caveat is precision: torch-webgpu operates at float32 while CUDA and MPS baselines use float16 with vendor-optimized libraries. This dtype mismatch inflates the reported gap—at matched float32, the CUDA gap narrows from 8.8× to 1.4× (mobile GPU), and MPS float32 (12.9 tok/s) falls below WebGPU, though this reflects MPS’s poorly optimized float32 code paths rather than WebGPU strength (Section D.3). We could not implement float16 in torch-webgpu because WGSL float16 compute support was not available on our hardware configuration. End-to-end LLM inference was tested across all three platforms: CUDA on Linux and Windows, MPS on macOS, CPU on all three, and browser-based WebLLM on Windows and macOS (Table 3, Appendix E). However, torch-webgpu specifically is validated on only one platform (RTX 5090/Dawn/Vulkan), so the over- head accounting (Section 4.4) rests on a single-system experiment. Cross-platform torch-webgpu testing— particularly on Metal, where fusion behaves differently (Section 7.8)—would strengthen these claims. We scope the overhead accounting to RTX 5090/Dawn; the benchmarks (four vendors, three backends, three browsers) provide broader validation.

## 4
Results

4.1 End-to-End Performance Table 2: End-to-end inference performance across backends (10–30 runs per configuration, 50 tokens/run) Backend Dtype Tok/s 95% CI CV TTFT (ms) vs CUDA Qwen2.5-0.5B-Instruct CUDA (compiled, RTX 5090) fp16 185.5 [184.2, 186.8] 0.9% 5.4 1.00× CUDA (eager, RTX 5090) fp16 182.9 [182.3, 183.5] 0.4% 5.5 0.99× MPS (Apple M2) fp16 47.8 [47.7, 48.0] 0.9% 20.9 0.26× torch-webgpu (fused, RTX 5090) fp32 21.0 [20.7, 21.4] 4.0% 41.6 0.11× CPU (AMD Ryzen, eager) fp32 13.7 [13.4, 14.0] 3.2% 72.8 0.07× ONNX Runtime (WebGPU, RTX 5090) fp32 13.1 [13.0, 13.2] 1.1% 73.5 0.07× Qwen2.5-1.5B-Instruct CUDA (eager, RTX 5090) fp16 155.3 [154.9, 155.6] 0.6% — 1.00× MPS (Apple M2) fp16 20.6 [20.4, 20.9] 2.9% — 0.13× torch-webgpu (fused, RTX 5090) fp32 17.9 [17.7, 18.2] 3.8% 51.3 0.12× torch-webgpu (unfused, RTX 5090) fp32 10.4 [10.4, 10.5] 0.9% 87.9 0.07× The “vs CUDA” column compares WGSL float32 against CUDA float16—see Section 3.6 for discussion of this confound and its implications. Per-operation overhead is consistent at ∼95–99 µs across both model sizes (Section 7.7), and fusion benefits grow with depth: 1.72× at 1.5B vs 1.56× at 0.5B, since more layers mean more fusible operations. Without fusion, torch-webgpu (13.5 tok/s) and ONNX Runtime (13.1 tok/s) perform almost identically (Section 6.3). Run-to-run variance is low (CV 0.9–4.0%). 4.2 Cross-Platform End-to-End Inference Table 3 shows end-to-end LLM inference across all platforms. Table 3: Cross-platform performance comparison (Qwen2.5-0.5B) Platform Processor Accelerator Tok/s 95% CI CV vs WebGPU Native GPU (end-to-end inference) Linux (primary) RTX 5090 CUDA 185.5 [184.2, 186.8] 0.9% 8.8× macOS† Apple M2 MPS 12.9 [12.6, 13.2] 5.5% 0.61× Windows 11 (laptop)† RTX PRO 2000 CUDA 30.1 [29.7, 30.5] 3.3% 1.4× CPU (end-to-end inference) Linux (primary) AMD Ryzen 9800X3D CPU 13.7 [13.4, 14.0] 3.2% 0.65× Windows 11 (laptop)† Intel Core Ultra 7 CPU 8.1 [7.9, 8.4] 8.7% 0.39× macOS† Apple M2 CPU 6.2 [6.1, 6.3] 4.7% 0.30× †Windows and macOS results use float32 precision (bench_e2e.py, 30 runs each). Linux results are from separate benchmarks: CUDA (float16, 10 runs) and CPU (float32, 10 runs). Float32 is included for dtype-matched comparison with torch-webgpu (also float32). For float16 baselines see Table 2: MPS achieves 47.8 tok/s (3.7× higher than float32). The most informative comparison is dtype-matched: the Windows laptop CUDA result (RTX PRO 2000, float32, 30.1 tok/s) achieves 1.4× WebGPU’s throughput despite ∼6× less compute, pointing to dis- patch/framework overhead as the dominant factor. The 8.8× Linux CUDA gap compares float32 against float16 and is unreliable (Section 3.6). MPS comparisons are similarly confounded: MPS float32 (12.9 tok/s) falls below WebGPU due to poorly optimized float32 code paths (3.7× penalty vs. float16). At float16, MPS (47.8 tok/s) exceeds WebGPU by 2.3×. Section 4.4 provides approximate overhead accounting. The fusion experiment (Section 4.4) provides cleaner causal evidence. 4.3 Forward Pass Breakdown For torch-webgpu, the average time per token generation cycle is approximately 48ms (total_time / tokens = 2.38s / 50). FX graph analysis of a single forward pass identifies 876 compute operations (out of 1,911 total nodes), dominated by elementwise operations and linear projections (detailed breakdown in Table 10, Appendix B). Not all FX operations become WebGPU dispatches, for instance shape operations (241 total) don’t require them. We don’t know the theoretical lower bound of actual dispatch count range. The upper bound assumes no fusion and has 876 dispatches, with our measured fused count at 564 (876 −312 saved by fusion). This bounds per-dispatch overhead at 0.08–0.35 ms from end-to-end TTFT. Direct measurement (Section 4.4) provides more precise values. 4.4 Direct Dispatch Measurement We implemented a C++ dispatch profiler (csrc/core/dispatch_profiler.cpp) that instruments encoder creation, bind group creation, and submission time per dispatch. WebGPU’s queue.Submit() is asyn- chronous, so CPU-side measurements do not directly sum to wall-clock time. On our RTX 5090, the unfused baseline runs at 71.4 ms TTFT with 876 operations. With fusion, TTFT drops to 41.6 ms with 564 dis- patches. The 29.8 ms saved across 312 fewer dispatches (Table 5) gives a derived overhead of ∼95 µs per operation. This figure is higher than the true per-dispatch cost because it includes Python/framework over- head: direct measurement via sequential dispatches (exp6/exp7) shows true per-dispatch cost of ∼24 µs (Dawn) to ∼36 µs (wgpu/Vulkan), with the remaining ∼59–71 µs attributable to Python interpreter and torch-webgpu framework code. We distinguish “per-dispatch cost” (directly measured WebGPU API cost) from “per-operation overhead” (total including framework). We sketch the following approximate overhead accounting as a qualitative ordering of bottlenecks, not a precise decomposition. Two of three components are derived quantities with ∼30% uncertainty, and the three factors are not independent. The fusion experiment above provides cleaner causal evidence (312 fewer dispatches, same kernels, 53% improvement). GPU-side kernel profiling is unavailable (Nsight operates below Dawn’s abstraction, and WebGPU timestamp queries give only pass-level granularity). Our CPU- side profiler (Table 20, Appendix M) shows submission dominating at 40% of per-dispatch cost. The three contributing factors, of which only the first is a property of WebGPU itself, are: (1) WebGPU dispatch cost (∼13.5 ms, ∼32% of TTFT): 564 dispatches × 24 µs, directly measured and inherent to the API regardless of host language, (2) Python/framework overhead (∼28–40 ms, derived): ∼59–71 µs per operation covering the Python interpreter, tensor metadata, and torch-webgpu framework code and (3) low baseline shader efficiency (Section 7.6): 1–2% of FP32 peak, though ∼17% is achievable (Nuss, 2024). The first two factors both respond to fusion. Table 4 summarizes measured and derived quantities. Table 4: Approximate TTFT overhead accounting (fused torch-webgpu, RTX 5090/Dawn, Qwen2.5-0.5B). Directly measured quantities are shown in bold. Derived estimates have ∼30% uncertainty and should be interpreted as a qualitative ordering of bottlenecks. Components are not additive due to GPU/CPU pipelining overlap. Quantity Value (ms) Type Source

## Directly measured

TTFT (fused) 41.6 Measured End-to-end benchmark TTFT (unfused) 71.4 Measured End-to-end benchmark Per-dispatch cost 0.024 Measured Sequential dispatch (exp6/exp7)

## Well-constrained derived quantity

Per-operation overhead 0.095 Deriveda (71.4−41.6) / 312 fewer ops Estimates (∼30% uncertainty) WebGPU dispatch component 13–20 Estimatedb 564 ops × (24–36 µs) Framework component 28–40 Estimatedc 564 ops × (95−dispatch) µs GPU/CPU overlap ∼12 Residuald Reduces effective overhead aWell-constrained: 29.8 ms saved / 312 fewer ops = 95.5 µs. bRange reflects dispatch cost variation across backends (24 µs Dawn, 36 µs wgpu/Vulkan). cFramework = (per-operation overhead −per-dispatch cost) × 564 ops. dResidual = sum of components −measured TTFT, attributed to GPU/CPU pipelining (GPU executes operation N while CPU prepares N+1), but this attribution is a hypothesis, not a direct measurement. In short, the total per-operation overhead (∼95 µs) is well-constrained. Its partition into dispatch (∼24–36 µs) and framework (∼59–71 µs) components has ∼30% uncertainty. Both respond to fusion. Sensitivity analysis: Qualitative conclusions hold under ±20% parameter variation (Appendix G).

## 5
Optimization Experiments

We evaluated application-level optimizations to determine whether the performance gap can be closed with- out WebGPU specification changes. 5.1 Kernel and Overhead Optimizations The 84× softmax speedup (45ms →0.54ms) confirms that individual kernels can be highly optimized, but end-to-end performance did not proportionally improve: per-operation overhead (dispatch + framework)— not kernel execution time—dominates the forward pass (Table 16, Appendix I). Argmax readback adds ∼11 ms per token (∼22% of per-token time). Device-side argmax did not reach significance on either backend (p = 0.35 Vulkan, p = 0.62 Metal; Appendix H) and we draw no conclusions from it. Command batching, buffer pooling, and bind group caching all provide negligible benefit because autoregressive generation forces per-token sync.

## 6
Analysis

6.1 Kernel Fusion We initially implemented fused kernels for elementwise patterns (fused_mul_silu, fused_add_silu, fused_add_gelu), yielding &lt;5% improvement as they save only 10–20 dispatches per forward pass. Larger structural fusions proved far more impactful. Without fusion, torch-webgpu (13.5 tok/s) performs at CPU speed, with the GPU’s compute advantage negated by per-dispatch overhead. With fusion (Table 5), 21.0 tok/s exceeds CPU while remaining 2.3× slower than Apple M2 MPS at float16 (Table 2; at float32 this reverses—see Section D.3). We implemented three key fusions: Table 5: Impact of kernel fusion (controlled progressive experiment∗) Configuration Dispatches saved Tok/s TTFT (ms) No fusion (baseline) — 13.5 71.4 + Fused RMSNorm (6→1) 240/fwd 19.4 46.6 + Fused MLP gate+up+silu (3→1) +48/fwd 20.5 43.3 + Fused K+V projection (2→1) +24/fwd 20.6 41.6 Total improvement 312 fewer +53% -41% ∗Values from controlled progressive experiment. The 30-run benchmark (Table 2) reports 21.0 tok/s for the fully fused configuration. The difference reflects run-to-run variance across separate experiments. The three fusions target distinct patterns. RMSNorm fusion (Zhang &amp; Sennrich, 2019) combines pow, mean, add(eps), rsqrt, mul(x), and mul(weight) into a single kernel, saving 240 dispatches per forward pass (24 layers × 2 RMSNorms). MLP fusion combines gate projection, up projection, and SiLU activation (Shazeer, 2020) into a single kernel (silu(xW T gate)⊙(xW T up)), saving 48 dispatches across 24 layers. K+V fusion (Ainslie et al., 2023) merges both projections (identical dimensions in grouped query attention) into a single tiled matmul, saving 24 dispatches. RMSNorm (+44%, p &lt; 0.001) and MLP (+6%, p &lt; 0.001) fusions are statistically significant and account for essentially all of the 53% improvement. K+V fusion (+0.5%, p = 0.42) did not reach significance and is included in Table 5 only as a negative result. The 53% total is driven by the first two fusions (288 of 312 dispatches saved). At batch=1 the eliminated intermediates total only ∼1.8 MB (&lt;1 µs at 1.8 TB/s), confirming the 29.8 ms improvement is entirely from per-operation overhead reduction. 6.2 Mega-Kernel Investigation: Extreme Fusion Mega-kernels, which are entire transformer blocks in single dispatches, did not reach significance (p &gt; 0.38; Appendix C) and we draw no conclusions from them. The tiled strategy (3 dispatches instead of 7, Appendix L) is a separate significant result: 2× on Metal (p &lt; 0.001) and 1.17× on Vulkan (p &lt; 0.01). 6.3 ONNX Runtime Comparison: Fusion Strategy, Not Implementation Quality Without fusion, torch-webgpu (13.5 tok/s) and ONNX Runtime (13.1 tok/s) perform very similarly—the 60% gap from fused torch-webgpu reflects model-specific kernel fusion.

## 7
Cross-Implementation Validation

Our initial experiments relied on a single WebGPU implementation (Dawn on NVIDIA). We therefore validated across multiple implementations and GPU vendors using wgpu-native (gfx-rs Community, 2024), a Rust WebGPU implementation supporting both Vulkan and Metal backends. 7.1 Experimental Setup We tested configurations across four GPU/iGPU vendors (NVIDIA, AMD, Apple, Intel). Native implemen- tations include Dawn (RTX 5090) and wgpu with Vulkan (RTX 5090, AMD iGPU) and Metal (Apple M2). Browser configurations span Chrome on RTX 5090/Linux/Vulkan, RTX 2000 laptop/Windows/D3D12, and Intel Core Ultra iGPU/Windows/D3D12, Safari on Apple M2/macOS/Metal and Firefox on three platforms (Apple M2/macOS, RTX 2000/Windows, Intel iGPU/Windows), all of which show identically elevated per- dispatch cost consistent with rate-limiting. 7.2 Per-Dispatch Cost Comparison Single-operation measurements include GPU-CPU synchronization overhead. Sequential measurements (multiple dispatches with sync only at the end) isolate the true per-dispatch cost. Table 6: Per-dispatch cost across WebGPU implementations: Single-op vs Sequential measurement Implementation Single-op (µs) Sequential (µs) Backend Native implementations (dispatch benchmarks) Dawn (RTX 5090) 496.8 23.8 Vulkan wgpu (RTX 5090) 35.8 35.8 Vulkan wgpu (AMD iGPU) 24.8 24.5 Vulkan wgpu (Apple M2) 48.3 71.1 Metal Browsers—practical (dispatch benchmarks; E2E via WebLLM in Table 13) Chrome (RTX 5090, Linux) 2071.2 32.8 Vulkan Chrome (RTX 2000, Win) 2728.8 58.7 D3D12 Chrome (Intel iGPU, Win) 3123.6 66.5 D3D12 Safari (Apple M2) 248.0 31.7 Metal Browsers—rate-limited† (impractical for ML) Firefox (Apple M2) 103,490 1038.7 Metal Firefox (RTX 2000, Win) 106,520 1036.7 D3D12 Firefox (Intel, Win) 104,328 1047.3 D3D12 †Observed behavior consistent with rate-limiting, but we did not examine Firefox source code to confirm the mecha- nism Desktop Vulkan shows ∼24–36 µs per-dispatch cost, consistent across four GPU vendors. Laptop/mobile GPUs are ∼2× higher (59–67 µs). Safari achieves 31.7 µs on Metal—2.2× lower than wgpu-native (71.1 µs). Firefox shows ∼1040 µs regardless of platform, consistent with rate-limiting (we did not examine Firefox source code). Single-op measurements overestimate by 10–60× due to sync conflation: Dawn’s ∼497 µs single-op includes ∼24 µs dispatch + ∼450 µs synchronization. 7.3 RMSNorm Fusion Across Implementations Table 7: RMSNorm fusion speedup across implementations (6 dispatches →1) Implementation Unfused (ms) Fused (ms) Speedup Backend wgpu (RTX 5090) 0.101 0.072 1.41× Vulkan wgpu (AMD iGPU) 0.106 0.063 1.67× Vulkan wgpu (Apple M2) 2.03 2.13 0.95× Metal Chrome (RTX 5090) 2.11 1.99 1.06× Vulkan Safari (Apple M2) 0.20 0.22 0.91× Metal Fusion benefits are backend-dependent: only Vulkan (native) yields significant 1.4–1.7× speedup. Metal shows no benefit (0.95× wgpu, 0.91× Safari). Vulkan (browser) yields 1.06×. Section 7.8 analyzes Metal’s fusion ineffectiveness. 7.4 Preliminary Mega-Kernel Investigation At small kernel scale (256×256), mega-kernels are inconclusive (p &gt; 0.38 on both backends, Table 11 in Appendix C). The tiled strategy (3 dispatches vs 7) yields 2× on Metal and 1.17× on Vulkan (Table 19). 7.5 CUDA Comparison: Per-Operation Overhead as Differentiating Factor To isolate whether the WebGPU–CUDA performance gap stems from per-operation overhead or kernel efficiency, we compared dispatch/launch overhead and fusion benefits (Table 17, Appendix J). CUDA fusion provides no benefit (0.92×) while WebGPU/Vulkan gets 1.4×. It suggests the gap is per-operation overhead, not kernel quality. 7.6 Kernel Compute Efficiency To isolate kernel compute efficiency from dispatch overhead, we measured WebGPU matmul throughput at production dimensions via wgpu/Vulkan benchmarks (30 sequential dispatches, sync only at end). At these dimensions, dispatch overhead is negligible relative to kernel execution time: for the MLP up projection (6.40 ms wall time), dispatch overhead contributes ∼36 µs (&lt;0.6%), so wall-clock time is a tight upper bound on kernel execution time. This is further supported by the timeline analysis (Table 20), where GPU synchronization time is only ∼0.5 µs per dispatch—indicating that GPU kernels complete well within the CPU-side dispatch interval. We therefore treat the wall-clock efficiency measurements below as kernel efficiency bounds: Table 8: WebGPU kernel compute efficiency (wgpu/Vulkan, RTX 5090, 30 runs) Operation Dimensions Time (ms) TFLOP/s % Peak† MLP up projection 896×896×4864 6.40 1.22 1.2% MLP down projection 896×4864×896 3.79 2.06 2.0% Toy matmul 256×256×256 1.10 0.030 &lt;0.1% †RTX 5090 non-tensor-core FP32 peak: 21,760 CUDA cores × 2 (FMA) × 2.41 GHz ≈105 TFLOP/s. Tensor cores are not accessible via WGSL. Our WGSL shader uses 16×16 tiling without bank-conflict-free shared memory access or vendor-specific optimizations. The 1–2% figure reflects our unoptimized WGSL shader, not a WGSL ceiling: third-party optimized WGSL achieves ∼17% of FP32 peak (Nuss, 2024), and webgpu-blas (Hidaka, 2024) reports 376 GFLOP/s. We did not implement these optimizations and report 1–2% as a baseline characterization. The fusion experiment (Table 5) cleanly isolates per-operation overhead from kernel quality: same kernels, fewer dispatches, 53% improvement. At 17% kernel efficiency, the MLP up projection would drop from 6.40 ms to ∼0.56 ms—below the 0.095 ms per-operation overhead—eliminating the ∼12 ms GPU/CPU overlap and making dispatch + framework overhead a larger fraction of TTFT. We scope this claim to batch=1: at batch=1 with the current dispatch-heavy pipeline, per-operation overhead dominates regardless of kernel quality. At larger batch sizes (Appendix F), kernel efficiency would matter more. The CUDA gap (8.8×, or 1.4× at matched dtype) reflects all factors simultaneously. 7.7 Model Size Scaling Per-operation overhead is consistent across model sizes (∼95 µs at 0.5B, ∼99 µs at 1.5B) and the WebGPU- to-CUDA ratio is stable (8.7–8.8×). Full scaling analysis in Appendix K. 7.8 Summary and Optimization Recommendations Table 9 summarizes the cross-implementation findings. Backend choice (Vulkan vs Metal) is the dominant factor for optimization behavior (fusion helps Vulkan but not Metal). For raw dispatch overhead, implementation also matters substantially within a backend: Safari Metal achieves 31.7 µs vs. wgpu-native Metal at 71.1 µs (2.2×), and Vulkan varies from 23.8 µs (Dawn) to 35.8 µs (wgpu). Across vendors on the same backend, variation is smaller: Vulkan ranges 24–36 µs across NVIDIA, AMD, and Intel. Optimizations that help on one backend may not transfer to the other. Table 9: Optimization recommendations by target backend

## Optimization
Vulkan
Metal
Notes

RMSNorm fusion (6→1) ✓1.4× × 0.95× Helps Vulkan only Tiled MLP (7→3 disp) ✓1.17× ✓2.0× Significant on both Command batching × minimal × minimal Sync per token negates benefit Safari/Metal’s RMSNorm regression (0.91×) despite low dispatch overhead (31.7 µs) suggests kernel-level causes. The tiled MLP strategy yields 2× on Metal (Appendix L), so this is pattern-specific. Numerical precision is validated within float32 limits (Appendix N). Inconclusive experiments are in Appendices C and H.

## 8
Related Work

ONNX Runtime (Microsoft, 2024) with WebGPUExecutionProvider and ORT_ENABLE_ALL graph optimiza- tion achieves 13.1 tok/s. The gap from fused torch-webgpu (21.0 tok/s) reflects architecture-specific fusion, not implementation quality (Section 6.3). Among browser ML frameworks, TensorFlow.js (Google, 2024c), MediaPipe (Google, 2024b), and Transformers.js (Hugging Face, 2024) support WebGPU but target different abstraction levels (we did not benchmark them). WebLLM (MLC-AI, 2024), built on TVM (Chen et al., 2018), achieves 46–51 tok/s decode with q4f16-quantized Qwen2.5 on Chrome (Appendix E)—∼2.4× faster than torch-webgpu, reflecting quantization (Frantar et al., 2023; Lin et al., 2024), graph compilation, and zero Python overhead. We weren’t able yet to isolate which of these factors contributes most to WebLLM’s advantage. A dtype-matched float32 WebLLM comparison, or running our models through WebLLM’s com- pilation stack, would be informative future work. WeInfer (Chen et al., 2025) reports 3.76× over WebLLM via buffer reuse and async pipelines, and WebNN (W3C Web Machine Learning Working Group, 2024) targets higher-level ML primitives with NPU support (browser adoption is still early). On the native side, server-side systems (vLLM (Kwon et al., 2023), SGLang (Zheng et al., 2024)) and on- device runtimes (llama.cpp (Gerganov, 2023) with aggressive quantization (Dettmers et al., 2022)) bypass both Python overhead and API abstraction, while TensorRT (NVIDIA, 2024c) and XLA (Google, 2024d) aggressively fuse operations via graph compilation, eliminating the per-dispatch overhead that dominates our measurements. Triton (Tillet et al., 2019) provides auto-tuned GPU kernel generation. WGSL (W3C GPU for the Web Working Group, 2024a) has no equivalent infrastructure, partly explaining the kernel efficiency gap (1–2% vs. achievable ∼17%). For GPU dispatch overhead specifically, Vulkan (Khronos Group, 2024) achieves 2–10× lower overhead than OpenGL, and CUDA (NVIDIA, 2024b) launch latency is 3–10 µs with CUDA Graphs (NVIDIA, 2024a) reducing to &lt;1 µs via capture/replay. Our measurements (WebGPU 24– 36 µs, CUDA 7.4 µs) are consistent. Pope et al. (Pope et al., 2023) characterize autoregressive decoding as memory-bandwidth-bound at batch=1, aligning with our finding that dispatch overhead—not compute— dominates at small batch sizes.

## 9
Implications

9.1 For WebGPU ML Deployment WebGPU ML inference is currently practical for latency-tolerant, portability-critical, privacy-sensitive or educational workloads—not for latency-critical or high-throughput serving where CUDA is available. The main caveat is these conclusions reflect batch=1 and float32 results. Batch inference could amortize dispatch overhead (Appendix F), and float16 support would narrow the gap. 9.2 For WebGPU Specification Significant performance improvements likely require specification-level changes: compute graph cap- ture/replay (analogous to CUDA Graphs), cooperative groups for fusion without sacrificing parallelism, persistent kernels, and reduced validation for trusted workloads. These must be weighed against browser security constraints; the current design prioritizes safety.

## 10
Reproducibility

Full environment specifications (software versions, driver details, branch pinning) are in Appendix O. Repos- itory: https://github.com/jmaczan/torch-webgpu. CPU-only benchmarks (benchmarks/portable/) re- quire no Dawn build. Raw results at benchmarks/results_*.json and experiments/results/*.json.

## 11
Conclusion

Our primary contribution is the cross-vendor characterization of WebGPU dispatch overhead: sequential- dispatch measurement reveals true per-dispatch cost (24–36 µs Vulkan, 31–71 µs Metal)—20× lower than naive single-op benchmarks—validated across four GPU vendors, three backends, three browsers, and four architectures. This finding is dtype-independent and API-inherent. We distinguish per-dispatch cost (We- bGPU API alone) from per-operation overhead (∼95 µs including Python/framework), with the latter being stack-specific. The fusion experiment provides the cleanest causal evidence: reducing 312 dispatches with unchanged kernel quality yields 53% end-to-end improvement, confirming per-operation overhead as the ac- tionable bottleneck at batch=1. Backend choice (Vulkan vs Metal) is the dominant factor for optimization behavior, though implementation choice also matters within a backend (2.2× for Metal). End-to-end infer- ence spans three platforms with CUDA, MPS, CPU, and browser baselines (Table 3). The torch-webgpu results (21.0/17.9 tok/s, RTX 5090/Dawn) provide illustrative context for one configuration. Key limitations: while end-to-end inference was tested across three platforms with five backend types (Ta- ble 3), the torch-webgpu backend and its overhead accounting rest on a single platform (RTX 5090/Dawn). The overhead accounting is approximate (∼30% partition uncertainty). Kernel efficiency (1–2%) is specific to our unoptimized shaders, though analytical projection suggests dispatch overhead would remain domi- nant even at 17% efficiency (Section 7.6). All results are batch=1 only. We think that per-dispatch cost is the finding most likely to generalize. Framework overhead and shader efficiency are stack-specific. All implementations, benchmarks, and raw data are open-sourced.

## Broader Impact Statement

WebGPU enables ML inference on hardware where CUDA is unavailable, broadening access to GPU- accelerated workloads and enabling on-device inference that keeps data local. The models we tested (0.5– 1.5B parameters) are far less capable than cloud-served alternatives, and browser sandboxing limits misuse. One consideration worth noting: Firefox’s ∼1040 µs elevated dispatch cost (behavior consistent with rate- limiting, though we did not confirm the mechanism via source code) perhaps mitigates GPU fingerprinting and cryptomining, and while this behavior is already observable, our characterization of it could inform eva- sion. Proposed spec enhancements (Section 9.2) would need browser vendor security review. Our repository includes RESPONSIBLE_USE.md. The techniques described are standard kernel fusion.

## References

Joshua Ainslie, James Lee-Thorp, Michiel de Jong, Yury Zemlyanskiy, Federico Lebrón, and Sumit Sanghai. GQA: Training generalized multi-query transformer models from multi-head checkpoints. In Proceedings of the 2023 Conference on Empirical Methods in Natural Language Processing (EMNLP), 2023. Jason Ansel, Edward Yang, Horace He, Natalia Gimelshein, Animesh Jain, Michael Voznesensky, Bin Bao, Peter Bell, David Berard, Evgeni Burovski, et al. PyTorch 2: Faster machine learning through dynamic Python bytecode transformation and graph compilation. In Proceedings of the 29th ACM International Conference on Architectural Support for Programming Languages and Operating Systems (ASPLOS), 2024. doi: 10.1145/3620665.3640366. Tianqi Chen, Thierry Moreau, Ziheng Jiang, Lianmin Zheng, Eddie Yan, Haichen Shen, Meghan Cowan, Leyuan Wang, Yuwei Hu, Luis Ceze, Carlos Guestrin, and Arvind Krishnamurthy. TVM: An automated end-to-end optimizing compiler for deep learning. In Proceedings of the 13th USENIX Symposium on Operating Systems Design and Implementation (OSDI), 2018. Zhiyang Chen, Yun Ma, Haiyang Shen, and Mugeng Liu. WeInfer: Unleashing the power of WebGPU on LLM inference in web browsers. In Proceedings of the ACM Web Conference (WWW), 2025. doi: 10.1145/3696410.3714553. 3.76× speedup over WebLLM via buffer reuse and asynchronous pipelines. Tri Dao. FlashAttention-2: Faster attention with better parallelism and work partitioning. In International Conference on Learning Representations (ICLR), 2024. Tri Dao, Daniel Y. Fu, Stefano Ermon, Atri Rudra, and Christopher Ré. FlashAttention: Fast and memory- efficient exact attention with IO-awareness. In Advances in Neural Information Processing Systems (NeurIPS), volume 35, 2022. Tim Dettmers, Mike Lewis, Younes Belkada, and Luke Zettlemoyer. LLM.int8(): 8-bit matrix multiplication for transformers at scale. In Advances in Neural Information Processing Systems (NeurIPS), volume 35, 2022. Elias Frantar, Saleh Ashkboos, Torsten Hoefler, and Dan Alistarh. GPTQ: Accurate post-training quanti- zation for generative pre-trained transformers. In International Conference on Learning Representations (ICLR), 2023. Georgi Gerganov. llama.cpp: LLM inference in C/C++. https://github.com/ggerganov/llama.cpp, 2023. Cross-platform CPU and GPU inference with quantization support. Accessed February 2026. gfx-rs Community. wgpu: Safe and portable GPU abstraction in Rust. https://wgpu.rs/, 2024. Pure-Rust WebGPU implementation with Vulkan, Metal, DX12, and OpenGL backends. Accessed February 2026. Google. Dawn: WebGPU implementation for Chromium. https://dawn.googlesource.com/dawn, 2024a. Accessed February 2026. Google. MediaPipe: Cross-platform, customizable ML solutions for live and streaming media. https:// ai.google.dev/edge/mediapipe/solutions/guide, 2024b. Supports WebGPU for browser deployment. Accessed February 2026. Google. TensorFlow.js. https://www.tensorflow.org/js, 2024c. Accessed February 2026. Google. XLA: Optimizing compiler for machine learning. https://www.tensorflow.org/xla, 2024d. Graph-level compilation with operator fusion and memory planning. Accessed February 2026. Masatoshi Hidaka. webgpu-blas: WebGPU-accelerated BLAS operations. https://github.com/ milhidaka/webgpu-blas, 2024. Reports 376 GFLOP/s for 4096×4096 matmul on Windows. Accessed February 2026. Hugging Face. Transformers.js: State-of-the-art machine learning for the web. https://huggingface.co/ docs/transformers.js, 2024. JavaScript library for running transformer models in the browser with WebGPU support via ONNX Runtime Web. Accessed February 2026. Khronos Group. Vulkan 1.3 specification. https://registry.khronos.org/vulkan/specs/1.3/html/, 2024. Explicit GPU API designed to reduce CPU overhead vs OpenGL. Accessed February 2026. Woosuk Kwon, Zhuohan Li, Siyuan Zhuang, Ying Sheng, Lianmin Zheng, Cody Hao Yu, Joseph E. Gon- zalez, Hao Zhang, and Ion Stoica. Efficient memory management for large language model serving with PagedAttention. In Proceedings of the 29th ACM Symposium on Operating Systems Principles (SOSP), 2023. doi: 10.1145/3600006.3613165. Ji Lin, Jiaming Tang, Haotian Tang, Shang Yang, Wei-Ming Chen, Wei-Chen Wang, Guangxuan Xiao, Xingyu Dang, Chuang Gan, and Song Han. AWQ: Activation-aware weight quantization for on-device LLM compression and acceleration. In Proceedings of Machine Learning and Systems (MLSys), 2024. Microsoft. ONNX runtime. https://onnxruntime.ai/, 2024. Accessed February 2026. MLC-AI. WebLLM: High-performance in-browser LLM inference engine. https://github.com/mlc-ai/ web-llm, 2024. WebGPU-accelerated LLM inference in the browser. Version used: 0.2.78 (February 2026). Zach Nuss. Optimizing a WebGPU matmul kernel for 1TFLOP+ performance. https://www. nuss-and-bolts.com/p/optimizing-a-webgpu-matmul-kernel, 2024. Achieves ∼1 TFLOP/s (∼17% of M2 Pro peak) via 2D tiling, loop unrolling, and vectorized FMA. Accessed February 2026. NVIDIA. CUDA graphs. https://developer.nvidia.com/blog/cuda-graphs/, 2024a. Graph capture and replay mechanism to amortize kernel launch overhead. CUDA kernel launch latency typically 3–10 µs; graph replay reduces to &lt;1 µs. Accessed February 2026. NVIDIA. CUDA C++ programming guide. https://docs.nvidia.com/cuda/ cuda-c-programming-guide/, 2024b. Section on asynchronous concurrent execution details kernel launch overhead and hardware queue management. Accessed February 2026. NVIDIA. TensorRT: High-performance deep learning inference optimizer and runtime. https://developer. nvidia.com/tensorrt, 2024c. Accessed February 2026. Reiner Pope, Sholto Douglas, Aakanksha Chowdhery, Jacob Devlin, James Bradbury, Jonathan Heek, Kefan Xiao, Shivani Agrawal, and Jeff Dean. Efficiently scaling transformer inference. In Proceedings of Machine Learning and Systems (MLSys), 2023. Qwen Team. Qwen2.5 technical report. arXiv preprint arXiv:2412.15115, 2024. Noam Shazeer. GLU variants improve transformer. arXiv preprint arXiv:2002.05202, 2020. Philippe Tillet, H. T. Kung, and David Cox. Triton: An intermediate language and compiler for tiled neural network computations. In Proceedings of the 3rd ACM SIGPLAN International Workshop on Machine Learning and Programming Languages (MAPL), 2019. doi: 10.1145/3315508.3329973. Ashish Vaswani, Noam Shazeer, Niki Parmar, Jakob Uszkoreit, Llion Jones, Aidan N. Gomez, Łukasz Kaiser, and Illia Polosukhin. Attention is all you need. In Advances in Neural Information Processing Systems (NeurIPS), volume 30, 2017. W3C GPU for the Web Working Group. WebGPU shading language (WGSL) specification. W3c working draft, World Wide Web Consortium, 2024a. URL https://www.w3.org/TR/WGSL/. Accessed February 2026. W3C GPU for the Web Working Group. WebGPU specification. W3c working draft, World Wide Web Consortium, 2024b. URL https://www.w3.org/TR/webgpu/. Accessed February 2026. W3C Web Machine Learning Working Group. Web neural network API. W3c candidate recommendation, World Wide Web Consortium, 2024. URL https://www.w3.org/TR/webnn/. Accessed February 2026. Samuel Williams, Andrew Waterman, and David Patterson. Roofline: An insightful visual performance model for multicore architectures. Communications of the ACM, 52(4):65–76, 2009. doi: 10.1145/1498765. 1498785. Biao Zhang and Rico Sennrich. Root mean square layer normalization. In Advances in Neural Information Processing Systems (NeurIPS), volume 32, 2019. Lianmin Zheng, Liangsheng Yin, Zhiqiang Xie, Chuyue Sun, Jeff Huang, Cody Hao Yu, Shiyi Cao, Christos Kozyrakis, Ion Stoica, Joseph E. Gonzalez, Clark Barrett, and Ying Sheng. SGLang: Efficient execution of structured language model programs. In Advances in Neural Information Processing Systems (NeurIPS), 2024. Note: Several analyses that provide important detail—including the per-dispatch timeline breakdown (Ap- pendix M), crossover analysis (Appendix F), and browser end-to-end results (Appendix E)—were moved to appendices. Readers interested in the fine-grained dispatch overhead structure should consult Table 20 in particular, as it provides more granular evidence than the main-text summary. All appendix material supports but is not essential for understanding the main contributions.

## A
Detailed Benchmark Results

Complete benchmark results are available in the repository at benchmarks/results_*.json. Each JSON file contains: • tokens_per_second: Mean throughput • tokens_per_second_std: Standard deviation • tokens_per_second_ci95: 95% confidence interval [lower, upper] • coefficient_of_variation: CV percentage • time_to_first_token_ms: TTFT measurement • time_to_first_token_ci95_ms: TTFT 95% CI • hardware: GPU/CPU specifications • runs: Number of timed runs (10–30) • warmup: Number of warmup runs (3–5) • all_tps: Individual tokens/sec for each run (for distribution analysis) • all_ttft_ms: Individual TTFT for each run

## B
FX Graph Analysis Details

The FX graph analysis script (benchmarks/analyze_fx_graph.py) captures PyTorch’s intermediate repre- sentation before compilation. Key findings: • Total FX nodes: 1,911 • Compute operations: 876 • Shape operations (no dispatch): 241 • Placeholder/output nodes: 293 • Other metadata: 501 The 49 occurrences each of pow, mean, and rsqrt correspond to the RMSNorm decomposition across 24 layers × 2 norms + 1 final norm = 49 total RMSNorm operations. Table 10: FX graph operation breakdown (sum = 876 compute ops)

## Category
Operations
Count

Linear (matmul) Q, K, V, O proj, MLP 169 Multiply RMSNorm weights, MLP gate 220 Add Residuals, biases 145 SDPA Attention per layer (Dao et al., 2022; Dao, 2024) 24 SiLU MLP activation 24 RMSNorm components pow, mean, rsqrt 147 Concatenation KV cache, rotary 97 Other neg, embedding, index 50 Total compute ops 169+220+145+24+24+147+97+50 876 The FX graph structure is deterministic for a given model and input shape. Repeated runs produce identical operation counts.

## C
Mega-Kernel Experiment Details

The mega-kernel experiment tests whether extreme fusion (entire transformer blocks in single dispatches) improves performance. Table 11: Mega-kernel vs multi-workgroup: Preliminary comparison at toy scale (256×256, 30 runs). Results are inconclusive for production dimensions. Platform Backend Mega (ms) Multi (ms) Speedup p-value Result RTX 5090 Vulkan 0.090 ± 0.03 0.085 ± 0.01 0.95× 0.43 Inconclusive Apple M2 Metal 1.45 ± 0.32 1.40 ± 0.02 0.97× 0.38 Inconclusive Neither comparison reaches statistical significance (p &gt; 0.05). At production scale (896×4864), a single mega-kernel workgroup (256 threads) would perform ∼17,000 sequential MADs per thread, while multi-dispatch approaches can use ∼65,000+ threads—likely making mega-kernels significantly slower. Implementation: The mega_mlp shader (csrc/ops/mega_mlp.cpp) computes RMSNorm, gate/up projec- tions, SiLU, down projection, and residual add in a single dispatch. A single workgroup is required because WebGPU lacks cross-workgroup synchronization (workgroupBarrier() is intra-workgroup only). Scale limitation: At production dimensions, a single workgroup (256 threads) would severely under-utilize the GPU. The tiled strategy (3 dispatches) preserves parallelism: 2× on Metal, 1.17× on Vulkan (Table 19). Production-scale matmul validation: We benchmarked tiled matmul (16×16 tiles, multi-workgroup) at production dimensions via wgpu/Vulkan on RTX 5090 (30 runs each): Table 12: WebGPU matmul at production vs toy dimensions (wgpu/Vulkan, RTX 5090) Dimensions (M×K×N) Workgroups Mean (ms) Std (ms) GFLOP/s 256×256×256 16×16 1.10 0.09 30.4 896×896×4864 (MLP up) 56×304 6.40 0.46 1216 896×4864×896 (MLP down) 56×56 3.79 0.02 2055 Production-scale matmul achieves 1.2–2.1 TFLOP/s vs 30 GFLOP/s at toy scale—40–68× improvement from better GPU utilization. The 256×256 comparison clearly operates in a different regime.

## D
Cross-Platform Benchmark Details

D.1 CPU End-to-End Inference (Qwen2.5-0.5B) CPU baselines across three platforms (10–30 runs, 50 tokens, float32): Linux/AMD Ryzen 9800X3D: 13.7 ± 0.44 tok/s (CV 3.2%), Windows/Intel Core Ultra 7: 8.1 ± 0.71 tok/s (CV 8.7%), macOS/Apple M2: 6.2 ± 0.29 tok/s (CV 4.7%). Windows CPU shows higher variance (CV 8.7%) due to background OS activity on the laptop we couldn’t prevent. D.2 Windows Laptop (CUDA + CPU + Browser) RTX PRO 2000 Blackwell, Intel Core Ultra 7, 8GB VRAM, float32: CUDA 30.1 tok/s (0.5B) / 17.7 (1.5B), CPU 8.1 / 3.1. CUDA 1.5B scaling: 59% of 0.5B, steeper than RTX 5090 (84%), reflecting lower memory bandwidth. Browser results in Appendix E. Raw data at results/windows_e2e/. D.3 macOS (MPS + CPU, Apple M2) MacBook Air, 16GB unified, macOS 26.2: Float16: MPS 47.8 tok/s (0.5B) / 20.6 (1.5B). Float32: MPS 12.9 (0.5B) / 6.4 (1.5B), CPU 6.2 / 2.3. The float16→float32 penalty is 3.2–3.7× for MPS, substantially larger than CUDA’s typical ∼1.5–2×. At float32, torch-webgpu exceeds MPS by 1.6× (0.5B) and 2.8× (1.5B), though this reflects MPS’s poorly optimized float32 code paths rather than WebGPU strength. Browser results in Appendix E. Raw data at results/macos_e2e/. E Browser Ecosystem: WebLLM End-to-End Inference We measured end-to-end browser inference using WebLLM (MLC-AI, 2024) with q4f16-quantized Qwen2.5 models. These results are not directly comparable to our float32 torch-webgpu measurements due to quantization and graph compilation differences. Table 13: Browser end-to-end LLM inference via WebLLM (q4f16 quantization, decode tok/s from runtime stats∗) Platform Browser Model Decode (tok/s) Prefill (tok/s) Backend Windows 11 (RTX PRO 2000 Blackwell, 8GB) Windows Chrome 144 Qwen2.5-0.5B 51.1 ± 5.9 ∼650 D3D12 Windows Chrome 144 Qwen2.5-1.5B 45.7 ± 6.3 ∼350 D3D12 Windows Firefox 147 Qwen2.5-0.5B 9.1 ± 0.03 ∼73 D3D12 Windows Firefox 147 Qwen2.5-1.5B 9.1 ± 0.03 ∼55 D3D12 macOS (Apple M2, 16GB unified memory) macOS Chrome 143 Qwen2.5-0.5B 46.4 ± 0.2 ∼510 Metal macOS Chrome 143 Qwen2.5-1.5B 36.0 ± 0.4 ∼225 Metal macOS Safari 26.2 Qwen2.5-0.5B 41.7 ± 0.5 ∼257 Metal macOS Safari 26.2 Qwen2.5-1.5B 29.7 ± 0.3 ∼93 Metal macOS Firefox 147 Qwen2.5-0.5B 9.6 ± 0.04 ∼77 Metal macOS Firefox 147 Qwen2.5-1.5B 9.6 ± 0.07 ∼58 Metal ∗Reliability caveat: WebLLM generated only 7 tokens per run (EOS termination), which is too short for reliable throughput measurement from wall-clock time. We report decode tok/s from WebLLM’s internal runtime statistics, which instrument the decode loop directly, but these values should be interpreted with caution: 7-token runs may not capture steady-state behavior, and the high variance in some configurations (e.g., Chrome/Windows ±5.9–6.3 tok/s) may partly reflect this limitation. Windows Firefox: 20 runs to confirm rate-limiting, all others: 10 runs. Chrome achieves 46–51 tok/s decode, Safari 30–42 tok/s, and Firefox shows consistently elevated latency (9.1–9.6 tok/s), consistent with the rate-limiting behavior observed in dispatch benchmarks. Chrome/Metal decode (46.4 tok/s) reaches 97% of MPS native float16 (47.8 tok/s), reflecting WebLLM’s optimizations compensating for WebGPU overhead. Linux/Vulkan failures: Chrome Canary on Linux/Vulkan (RTX 5090) failed with shader compilation errors for Qwen models. Other models required the shader-f16 extension. D3D12 (Windows) succeeds where Vulkan (Linux) fails, suggesting shader compatibility varies by backend.

## F
Dispatch-Bound Crossover Analysis

We model the transition from dispatch-bound to compute-bound execution (Williams et al., 2009) as a function of batch size. For a linear layer with dimensions [B, din] × [din, dout]: • Compute time: Tcompute(B) = 2·B·din·dout throughputWGSL , where throughputWGSL ≈2 TFLOP/s (measured, Table 8) • Per-operation overhead: Toverhead ≈95 µs (dispatch + framework, independent of batch size) The crossover batch size B∗where compute time equals per-operation overhead is: B∗= Toverhead · throughputWGSL 2 · din · dout Table 14: Dispatch-bound crossover batch size B∗for representative operations Operation Dimensions (din × dout) B∗(computed) Regime at B=1 Qwen2.5-0.5B (896 hidden, 4864 intermediate) Attention Q/K/V proj 896 × 896 119 Overhead-bound MLP up projection 896 × 4864 22 Overhead-bound MLP down projection 4864 × 896 22 Overhead-bound Qwen2.5-1.5B (1536 hidden, 8960 intermediate) Attention Q/K/V proj 1536 × 1536 40 Overhead-bound MLP up projection 1536 × 8960 7 Overhead-bound MLP down projection 8960 × 1536 7 Overhead-bound Computed as B∗= (95 × 10−6 × 2 × 1012)/(2 · din · dout). At batch sizes below B∗, per-operation overhead dominates, above B∗, kernel compute dominates. At batch=1, all operations are deeply overhead-bound (B∗≥7 even for the largest matmuls). The per-operation overhead analysis holds across model sizes for single-request inference. Crossover to compute- bound behavior requires batch sizes ≥7–119 depending on the operation. Relation to roofline analysis: This crossover analysis serves a purpose analogous to the roofline model (Williams et al., 2009): just as a roofline plot identifies whether an operation is compute-bound or memory-bound, our crossover table identifies whether each operation is compute-bound or overhead- bound. At batch=1, all operations lie deep in the overhead-bound regime, making per-dispatch cost the binding constraint rather than arithmetic throughput or memory bandwidth. Limitation: All end-to-end results use batch=1, crossover predictions are analytical only. Memory band- width saturation, cache effects, and attention’s quadratic scaling could shift crossover points. Batch&gt;1 validation is the highest-priority future work.

## G
Sensitivity Analysis

The approximate three-factor overhead accounting (Section 4.4) depends on two derived quantities: per- operation overhead (∼95 µs, well-constrained from the fusion experiment) and its partition into dispatch vs. framework components. Using the upper-bound dispatch cost (36 µs instead of 24 µs) shifts the framework- to-dispatch ratio from 3× to 1.7× but does not change which factor dominates. Varying per-operation overhead by ±20% (76–114 µs) moves the framework estimate between ∼22–45 ms. At the lower bound framework and dispatch become comparable, but in all cases both still respond to fusion. Increasing the GPU/CPU overlap estimate to ∼20 ms (from ∼12 ms) only affects the overlap attribution, not the per- operation overhead itself. The qualitative conclusions are stable across these variations: per-operation overhead dominates TTFT, fusion is the most effective intervention, and baseline shader efficiency is a separate bottleneck.

## H
Device-Side Argmax Analysis

We implemented device-side argmax to reduce GPU→CPU synchronization overhead. Cross-platform results show this optimization is implementation-specific: Table 15: Device-side argmax: Cross-platform comparison (30 runs) Platform Full Readback (ms) Device Argmax (ms) Improvement p-value wgpu/Vulkan (RTX 5090) 0.42 ± 0.08 0.12 ± 0.03 +71%† 0.35 wgpu/Metal (Apple M2) 1.59 ± 0.12 1.69 ± 0.15 -7% 0.62 †Inconclusive: Despite the large point estimate (71%), this result does not reach statistical significance (p = 0.35) due to high variance in full readback measurements. The improvement direction is consistent across runs but the magnitude is unreliable. We report this as a suggestive but unconfirmed result. Explanation: Vulkan has low mapping overhead (∼0.1 ms) where data transfer reduction helps. Metal has high fixed mapping overhead (∼1.8 ms), so reducing size provides no benefit. Both results are inconclusive.

## I
Kernel optimizations

Table 16: Optimization results summary (30 runs, RTX 5090) Optimization Implementation Isolated Result E2E Impact

## Kernel optimizations

Parallel softmax Shared memory, 256 threads 84× (p &lt; 0.001) Bottleneck removed Tiled matmul 16×16 tiles 2–3× (p &lt; 0.001) &lt;5% improvement Overhead reduction attempts (null results) Command batching 16 dispatches per submit ∼0% No effect∗ Buffer pooling Size-class reuse ∼0% No effect Bind group caching Hash-based lookup ∼0% No effect ∗Autoregressive generation forces GPU→CPU sync per token, flushing batched commands.

## J
CUDA vs WebGPU Comparison

Table 17: CUDA vs WebGPU: Overhead and fusion comparison (sequential measurement) Metric CUDA WebGPU (Vulkan) Kernel launch/dispatch overhead 7.4 ± 9.2 µs 24–36 µs Overhead ratio 3–5× (WebGPU higher) RMSNorm unfused 21.3 µs — RMSNorm fused 23.2 µs — RMSNorm compiled (torch.compile) 20.9 µs — Fusion speedup 0.92× (no benefit) K Model scaling from 0.5B to 1.5B Table 18: Model size scaling: 0.5B vs 1.5B, both measured end-to-end (RTX 5090, batch=1 decode, 30 runs) Metric 0.5B (measured) 1.5B (measured) Scaling Source Layers 24 28 1.17× Model config Ops/forward (fused) 564 ∼658a 1.17× Scaled by layers WebGPU tok/s (fused) 21.0 17.9 0.85× Measured (30 runs) WebGPU tok/s (unfused) 13.5 10.4 0.77× Measured (30 runs) WebGPU TTFT fused (ms) 41.6 51.3 1.23× Measured WebGPU TTFT unfused (ms) 71.4 87.9 1.23× Measured Fusion speedup 1.56× 1.72× — More fusible ops Per-op overheadb ∼95 µs ∼99 µs ∼1.0× Fusion-derived CUDA tok/s (RTX 5090) 185.5 155.3 0.84× Measured (30 runs) MPS tok/s (Apple M2) 47.8 20.6 0.43× Measured (30 runs) aDispatch count scales linearly with layers (28/24 × 564 ≈658). Validated: exp12 benchmark gives 196 fused dispatches for 1.5B (7 ops/layer × 28). b1.5B per-op overhead: (87.9−51.3 ms) / 369 fewer ops ≈99 µs, consistent with 0.5B’s 95 µs.

## L
Multi-Dispatch Tiled Strategy

We evaluated a middle-ground approach between fully unfused (7 dispatches) and mega-kernel (1 dispatch): a tiled strategy using 3 dispatches that preserves parallelism while reducing dispatch count. This experiment is implemented in exp3_tiled_mega.py and results are in results/exp3_tiled_mega.json. Table 19: Multi-dispatch tiled strategy: Cross-platform comparison (MLP block, 30 runs) Platform Unfused Tiled Mega Tiled Speedup p-value† (7 disp) (3 disp) (1 disp) vs Unfused wgpu/Vulkan (RTX 5090) 0.72 ms 0.62 ms 0.32 ms 1.17× &lt;0.01 wgpu/Metal (Apple M2) 5.74 ms 2.85 ms 1.48 ms 2.01× &lt;0.001 †p-value for tiled vs unfused comparison. The 2× Metal speedup vs 1.17× Vulkan tracks the per-dispatch overhead difference (71 µs vs 25–36 µs): fusion matters more when dispatch is expensive.

## M
Timeline Analysis

CPU-side timing for 100 consecutive dispatches breaks down per-dispatch overhead as follows: Table 20: Per-dispatch timing breakdown (wgpu/Vulkan, 100 dispatches) Operation Total (µs) Per-dispatch (µs) Encoder create 644.9 6.4 Pass begin 317.1 3.2 Set pipeline 144.9 1.4 Set bind group 99.2 1.0 Dispatch call 63.9 0.6 Pass end 66.6 0.7 Encoder finish 614.5 6.1 Submit 1294.2 12.9 Total CPU time 3245.2 32.5 Wall clock time 3412.7 34.1 GPU sync time 50.3 0.5 Observation: submit dominates (40% of per-dispatch overhead), making command buffer submission the primary bottleneck. Pipeline/bind group setup contribute minimally after initial creation.

## N
Numerical Precision Validation

All tested operations (RMSNorm, MatMul, Softmax) match CPU reference within float32 limits (max ab- solute difference &lt; 2 × 10−4, see exp10_numerical_precision.py). No precision issues were observed.

## O
Reproducibility Environment

Our experiments used the following exact software versions: • torch-webgpu: Branch benchmark_and_optimize • Dawn: Branch main, commit 99986ea5e8 • PyTorch: 2.9.1+cu128 • ONNX Runtime: 1.24.0.dev20251218001 • Python: 3.12 • NVIDIA Driver: 570.124 • OS: Ubuntu 24.04, kernel 6.14.0-37-generic
